# AGENTS.md

**本文件不放内容，只做指路。全部工作约定、架构说明和踩过的坑都在
[CLAUDE.md](CLAUDE.md) —— 动手前请完整读那一份。**

## 为什么是一行指针，而不是一份副本

这份文件原本是 `CLAUDE.md` 的整份复制（204 行）。**它在被创建后的两小时内就已经漂了**：
`CLAUDE.md` 里加了「战役模式整体暂缓」那条约束，副本没有跟上——
于是 Codex 自己的规则文件里，恰恰缺了用户刚下的那条指令。

这正是本项目**头号病因**的又一次复现。`CLAUDE.md` 的「踩过的坑」里写着：

> **「同一份知识两份实现」是本项目的头号病因，已经出过七次 bug。**
> 前三个都不报错，只是**静默地算出另一套世界**。
> **文档写清楚防不住这类 bug，只有「共用同一个函数」能防住。**

那条规则原本讲的是代码（`battle.js` 和 `sim.js` 各写一份战斗逻辑），
但它对文档同样成立，而且后果更隐蔽：代码漂移至少还有测试可能抓到，
**两份规则文件漂移之后，两个 agent 会按不同的约束干活，没有任何东西会报错。**

所以这里不复制，只指路。给 Codex 的等价物就是「共用同一个函数」。

## 要改约定时

**改 `CLAUDE.md`，不要改这里。** 这份文件只在「Codex 专属、且对 Claude 不成立」
的规则出现时才需要新增内容——目前没有这种规则。
