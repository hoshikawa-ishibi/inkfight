# 合并两套 AI + 让 AI 学会配合 · 计划

> 持久化执行计划，供 context 重置后的新对话继续。
> 每完成一步把 `[ ]` 改成 `[x]`，并在末尾「进度记录」补一行。
> 计划中途发现假设错误就直接改本文件，不要另开一份。
> 前一份计划见 `REFACTOR_PLAN.md`（已完成）。

---

## ▶ 当前进度：Phase 1～4 已完成，**只剩 Phase 5（收尾 + 浏览器实测）**

接手时先跑一遍确认基线：

```bash
npm test          # 应为 87/87 通过
npm run balance   # 应能跑完并输出胜率表
git log --oneline -3
```

**已经做完的**（都已提交）：
- `ai-scoring.js` 是技能评分的唯一实现，`ai.js` 与 `sim.js` 共用
- `ai.js` 已脱离浏览器，是纯函数，只做难度包装（噪声 / tempo / teamwork 权重）
- AI 会配合了：集火、不重复上 buff、护住残血队友、按产出选治疗目标、
  不用大招收残血。配合强度由 `teamwork` 权重分档（easy 0 / normal 0.5 / hard 1）

- `sim.js` 直接调 `aiHard`，平衡测试测的就是玩家面对的 AI

**下一步是 Phase 5**：更新文档、浏览器实测三档难度各一局。

几条对接手有用的经验：
- 难度梯度**不能只靠随机噪声**。实测噪声从 10 加到 45，普通与困难的决策质量
  也只差 2.3——最优技能分数远高于次优，噪声改变不了排序。现在靠的是
  `tempo` 权重（算不算得清机会成本）。
- 验证 AI 行为要用**有真实权衡的角色**（守卫、牧师这类带辅助技能的）。
  用剑士测不出差异——它四个技能里三个是伤害，最优解太明显。
- 写涉及伤害的测试时注意**闪避会让断言随机失败**。给目标 `dodge = 0`。

## 背景

项目里有**两套互相独立的 AI 技能评分实现**：

| | 位置 | 服务对象 |
|---|---|---|
| A | `ai.js` 的 `scoreSkill` | 玩家对战的 AI（easy/normal/hard） |
| B | `sim.js` 的 `scoreSkill` | 平衡测试（`npm run balance`） |

带来两个后果：

1. **平衡数据测的不是玩家面对的 AI。** `npm run balance` 用的是 B，玩家打的是 A，
   两者决策逻辑完全不同，所以胜率结论未必适用于真实对局。
2. **改一处忘另一处就漂移。** 2026-08-24 给「狂暴」加 `power` 时只改了 B 和
   `battle.js`，漏了 A，结果 AI 放狂暴不造成伤害、玩家放却会（已修）。

这是本项目反复出现的同一个病根——同一个知识存在多份实现。此前已经修过两次：
战斗规则（`battle.js` vs `sim.js`，导致术士整套机制在统计里失效）、
buff 强度（三份硬编码）。这是第三次，也是最后一处。

### 有利条件（已调研确认）

- `ai.js` **import 了 `gameState` 但从未使用**，唯一的外部依赖是注入的 `_previewDmg`。
  也就是说它已经几乎是纯函数，把注入换成显式参数后即可在 Node 中运行。
- `battle.js` 的 `previewDmg(u,s)` 只是 `combat.js` 的 `previewDmg(u,s,scene)` 的包装，
  差别仅在于 `scene` 从 `gameState` 取。把 `scene` 提为参数即可解耦。
- 调用点很少：`battle.js:360-362` 调 ai 三档，`main.js:473` 调 `initAi`。

### 两套评分的取舍

- A（`ai.js`）用**经验分**（`score=22`/`30`/`80`），有三档难度差异，覆盖 17 种技能类型。
- B（`sim.js`）用**等效伤害**统一尺度，并含两项 A 没有的关键建模：
  - 机会成本 `tempo`（辅助技能占掉一整回合的代价）——补上它使平衡跨度从 21 收窄到 7.3
  - 条件性技能在条件不满足时给 0 分（净化无 debuff 可清、腐化爆发无层）

**决定：以 B 的等效伤害体系为核心**（更有原则、可解释、已被数据验证），
把 A 的难度差异改造成核心评分之上的"包装层"。

## 目标

1. 只保留一套技能评分，`ai.js` 和 `sim.js` 共用。
2. `npm run balance` 直接驱动玩家面对的 AI（hard 难度），让平衡数据真实可信。
3. 保留三档难度手感（简单要"笨"得可信，困难要有压迫感）。
4. AI 学会基本配合，不再各打各的。

## 非目标（避免范围膨胀）

- 不做搜索式 AI（минимакс/MCTS）。回合制 2v2 用评分式足够，且要保持零依赖。
- 不追求"最优解"AI。目标是**像个懂配合的对手**，不是不可战胜。
- 不改 `combat.js` 的战斗规则本身。
- 不引入任何新的 npm 依赖。

---

## Phase 1 — 让 `ai.js` 脱离浏览器

目的：`ai.js` 能在 Node 里跑，才谈得上被 `sim.js` 复用。

- [x] `ai.js` 删除 `import { gameState }`（确认无使用后直接删）
- [x] 删除 `initAi()` 与模块级 `_previewDmg`，改为 `import { previewDmg } from './combat.js'`
- [x] `aiEasy/aiNormal/aiHard` 与内部 `scoreSkill` 签名加 `scene` 参数：
      `aiHard(u, enemies, allies, scene)`
- [x] `battle.js:360-362` 调用处传 `gameState.scene`
- [x] `main.js` 删除 `initAi(previewDmg)` 调用与 import
- [x] 加测试 `test/ai.test.js`：在 Node 中直接调用 `aiHard`，断言它能返回
      合法的 `{skill, target}`（这条测试本身就证明了"已脱离浏览器"）

**验证**：`npm test` 通过；浏览器里人机对战仍正常（AI 会出手、不报错）。

## Phase 2 — 抽出共享评分模块 `ai-scoring.js`

- [x] 新建 `ai-scoring.js`，把 `sim.js` 现有的 `scoreSkill` 整体迁入并导出
      （含 `tempo` 机会成本、条件性技能归零、`BUFF_DEFAULTS` 读取等全部逻辑）
- [x] 同时迁入 `pickTarget` 的目标选择逻辑（净化找真正带 debuff 的、
      治疗按缺失血量、加攻给输出最高的）
- [x] `sim.js` 改为从 `ai-scoring.js` import，删除本地副本
- [x] `ai.js` 的 `scoreSkill` 改为调用共享评分，难度差异改为**包装层**：
  - `easy`：`score * 0.6 + 大随机噪声`，且 70% 概率直接选普攻（保留现有手感）
  - `normal`：`score + 中等噪声`
  - `hard`：`score + 极小噪声` + 战术加成（见 Phase 3）
- [x] 删除 `ai.js` 里与共享评分重复的经验分逻辑
- [x] 加测试：同一局面下 `hard` 的选择应当不劣于 `easy`（用固定随机种子或多次采样统计）

**验证**：`npm test` 通过；`npm run balance` 跑得通且结果与 Phase 2 前差异合理。

## Phase 3 — 让 AI 学会配合

配合的载体：新增一个**队伍战术上下文**对象，在同一支队伍的单位之间共享。
配合行为统一由 `opts.teamwork`（0~1）加权，三档难度取 0 / 0.5 / 1。

- [x] `ai-scoring.js` 新增 `makeTeamContext()`。**字段只剩 `{ focusTarget }`**
      ——原计划的 `buffedThisRound` / `lastActedRound` 实现时发现是多余的，
      理由见下方「计划修正」。
- [x] `battle.js` 与 `sim.js` 各自为两支队伍持有一个 context
      （battle.js 在 `startBattle()` 重建，sim.js 每局新建）。
      `focusTarget` 目标死亡时在 `focusFoe` 里自动清空，不需要按回合重置。
- [x] **集火**：目标改按「威胁 / 有效血量」排序（`killValue`），选定后写入
      context，除非新目标好 25% 以上否则不换人。护盾与减伤都折进有效血量。
- [x] **不重复增益**：`buffTarget` 优先选没带同类 buff 的队友；全队都带着时
      分数打到 15%，`tempo` 一减就变负分，AI 自然改做别的事。
- [x] **保护残血队友**：`protect` 项下沉到共享评分（嘲讽 / 铁壁），
      按 teamwork 加权；挂着嘲讽时加盾额外 ×1.6。`ai.js` 的同类加成已删除。
- [x] **治疗优先级**：`healPriority` = 实际治疗量 × 紧迫度 × 队友产出权重。
      **产出权重只用于排序，不进分数**——否则会连带把治疗整体抬价。
- [x] **补刀协同**：改为**同一单位内部的过量杀伤规避**，见「计划修正」。
- [x] 加测试：新增 `test/ai-teamwork.test.js`（25 条），每组配合行为都同时
      断言「困难会配合」和「简单不会」，防止难度梯度被悄悄抹平。

### 计划修正（实现时发现原假设不成立）

1. **`buffedThisRound` / `lastActedRound` 是多余的。** 技能立即结算，队友干了
   什么直接写在单位身上（buff 已挂上、目标已阵亡），从单位状态读比另记一份
   账更准。而且「回合」在两边含义不同：`battle.js` 一个回合只走「我方一个
   单位 + 敌方一个单位」，同队两个单位分属相邻的两个回合，按 round 清空
   永远清在错的时机。**集火是唯一真正需要跨单位记忆的行为**，因为「整队
   决定先杀谁」没法从战场状态反推。
2. **「让队友补刀」在这个回合结构下是错的。** 原计划设想同队单位在同一回合
   内连续行动，实际是敌我交替、且伤害立即结算——我不补的那一刀，意味着残血
   敌人多活整整一个回合并再打一次。真正的浪费是**同一个单位用大招收残血**
   （40 点伤害砸在 5 血目标上）。因此改为在 `damageWorth` 里把伤害按目标
   有效血量封顶，并按溢出比例扣技能 SP 成本，让免费普攻去收人头。

**验证**：`npm test` 87/87；`npm run balance 10000` 跑通、采样极差 2.0%。
浏览器实测放在 Phase 5 一起做。

## Phase 4 — 让平衡测试驱动真实 AI

- [x] `sim.js` 的 `pickSkill/pickTarget` 替换为直接调用 `aiHard(u, foes, friends, scene, ctx)`
- [x] 删除 `sim.js` 中剩余的评分副本代码（`pickSkill` 整个删掉，206 行）
- [x] `balance-report.mjs` 输出里标注"AI：hard（与玩家对战一致）"，
      并新增「节奏」一行（平均回合数 / 超时率）作为健康指标
- [x] 重新跑 `npm run balance 10000`，新基线已记入 `CLAUDE.md`

**验证**：`npm test` 87/87；采样极差 3.7%（< 10%）；
`grep -c "function scoreSkill" ai-scoring.js ai.js sim.js` → `1 / 0 / 0`。
另跑了技能使用率诊断确认没有新盲区：净化 / 腐化爆发在条件满足时使用率 100%，
「狂暴」0% 是技能本身算不过账（属角色数值问题，不在本计划范围内）。

## Phase 5 — 收尾

- [ ] 更新 `CLAUDE.md`：删除「已知架构风险：存在两套 AI」一节，
      改为记录新架构与新平衡基线
- [ ] 更新模块表，加入 `ai-scoring.js`
- [ ] 浏览器实测三档难度各一局，确认手感差异仍在、无报错、全程静音、测完关标签页

---

## 工作量与中断策略

- **Phase 1** 小（~30 分钟）。改动集中、风险低。做完即可独立提交。
- **Phase 2** 中（~1 小时）。是整个计划的核心，做完就消除了"两份实现"。
- **Phase 3** 中偏大（~1.5 小时）。属新功能，可细分，每个配合行为都能单独做完就提交。
- **Phase 4** 小（~30 分钟）。但会改变平衡数字，需要重新建立基线。
- **Phase 5** 小。

**每个 Phase 结束时代码都应处于可提交、测试全绿的状态**，方便随时中断。
若中途 context 重置，从本文件未勾选的第一项继续即可。

## 风险与应对

- **风险：Phase 4 后平衡数据大幅变化，之前的调值可能不再合适。**
  应对：这是预期内的——新数字才是对的。不要为了让数字"看起来没变"而回退。
  必要时按新基线重新评估角色强弱，但那属于下一轮工作。
- **风险：难度差异被抹平，简单难度变得太聪明。**
  应对：Phase 2 的包装层要保留 easy 的"偏爱普攻 + 大噪声"，
  并在 Phase 5 人工实测确认手感。
- **风险：配合逻辑让 AI 过强，玩家体验变差。**
  应对：配合行为主要加在 hard 上，normal 只吃部分，easy 不吃。

## 进度记录

- 2026-08-24：完成调研（确认 `ai.js` 未真正使用 `gameState`、依赖极少、调用点仅两处），
  写下本计划。
- 2026-08-24：**Phase 1 完成。** `ai.js` 已脱离浏览器——删掉未使用的 `gameState` import、
  移除 `initAi()` 注入改为直接 import `combat.js` 的 `previewDmg`、三个 ai 函数与
  `scoreSkill` 统一加 `scene` 参数。调用方 `battle.js` 传 `gameState.scene`，
  `main.js` 删除 `initAi` 调用。新增 `test/ai.test.js`（9 条），该文件能在 Node 里
  import 并调用 ai.js 本身即是「已脱离浏览器」的证明。
  验证：npm test 59/59；浏览器困难难度实测两回合，AI 正常决策（第二回合术士放瘟疫），
  场景加成显示正确，零控制台报错。
- 2026-08-24：**Phase 2 完成。** 新建 `ai-scoring.js`（200 行），`scoreSkill` 与
  `pickTarget` 只此一份，`ai.js` 与 `sim.js` 共用。`ai.js` 105→107 行（现在只剩
  难度包装），`sim.js` 390→209 行。
  **难度梯度的实现方式换了两次才对**：
  1) 最初只靠 noise 区分，实测普通与困难的决策质量只差 0.4，噪声加到 45 也只差 2.3——
     因为最优技能分数远高于次优，噪声改变不了排序。
  2) 改为让低难度「算不清机会成本」（tempo 开关），但直接归零会矫枉过正：
     守卫在 tempo=0 时 98% 的回合都在开护盾，满血狂加盾，质量反而低于只会平A的简单难度。
  3) 最终改为**权重制** tempo=0.35/0.7/1，梯度才正常。
  实测（守卫，满血满蓝局面）：简单 15.7 → 普通 28.3 → 困难 28.5，选辅助技比例
  14% → 1% → 0%。新增 3 条难度梯度测试。
  另修一条 flaky 测试：断言「有目标就应造成伤害」时用了自带 5% 闪避的剑士，
  有 5% 概率随机失败，已清零 dodge。连跑 5 次确认稳定。
  验证：npm test 62/62；`npm run balance` 正常；浏览器困难难度实测，
  AI 牧师选择「祝福→术士」（给输出最高的队友加攻），零报错。
- 2026-08-24：**Phase 3 完成。** 队伍战术上下文 + 五类配合行为，
  新增 `test/ai-teamwork.test.js`（25 条），npm test 87/87。
  **平衡数据被这一轮改动挖出两个 AI 盲区**（都是评分本身的错，不是角色强弱）：
  1. **敌方奶妈永远不会被列入集火名单。** 威胁度最初只算 `atk × 最高倍率`，
     牧师 atk 10、倍率 1.0，是全场威胁最低的单位，于是 10000 局里几乎没被
     针对过，胜率飙到 **65.2%**。改成 `max(伤害产出, 治疗量)` 后回落。
     一个没人管的牧师每回合抵消 48 点伤害，那它就值 48 点威胁。
  2. **「优先救输出高的队友」把治疗整体抬价了。** 产出权重（最高 ×1.5）
     本该只影响「救谁」，我一开始直接乘进了分数，等于让牧师觉得治疗这个
     技能变强了 50%，于是滥治疗。拆成 `healGain`（分数）和 `healPriority`
     （排序）两个函数。
  **教训：给「谁更重要」建模时，排序用的权重和绝对分数必须分开**——
  混在一起会让「在 A、B 之间选 A」悄悄变成「A 这件事整体更值得做」。
  当前平衡（10000 局，采样极差 2.0%）：牧师 60.9% / 弓手 55.7% / 术士 53.5% /
  守卫 53.1% / 剑士 48.5% / 法师 47.1% / 狂战士 41.3% / 刺客 39.8%。
  跨度从 11.6 拉开到 21.1——**AI 变强会放大角色间的差距**（集火让脆皮更吃亏、
  不浪费回合让辅助更受益）。数值调整留到 Phase 4 换成真 AI、拿到最终基线之后，
  现在调等于白调。
- 2026-08-24：**Phase 4 完成。** `sim.js` 删掉最后一份 `pickSkill`，
  直接调 `aiHard(u, enemies, allies, scene, ctx)`。**本计划的核心目标达成：
  从 data.js 到评分到决策只剩一条链路，平衡测试测的就是玩家面对的 AI。**
  `balance-report.mjs` 新增「节奏」一行（平均回合数 / 超时率），
  `runSimulation` 的 `onDone` 多带一个 `meta` 参数（老调用方不受影响）。
  **新基线（10000 局，AI=hard，采样极差 3.7%，平均 8.7 回合、0.5% 超时）**：
  牧师 63.6% / 弓手 55.3% / 术士 54.0% / 守卫 53.3% /
  剑士 46.3% / 法师 45.2% / 刺客 43.2% / 狂战士 39.1%，跨度 24.5。
  跑了技能使用率诊断确认没有新盲区（净化 / 腐化爆发条件满足时 100% 使用）。
  狂战士垫底有明确原因：它的攻击力随掉血上升、有效血量随掉血下降，
  在「威胁 / 有效血量」的集火模型里越打越优先，形成死亡螺旋。
  **角色数值调整属于下一轮工作，不在本计划范围内。**
