# 重做战役模式：难度曲线 + 剧情 + 角色绑定 · 计划

> 持久化执行计划，供 context 重置后的新对话继续。
> 每完成一步把 `[ ]` 改成 `[x]`，并在末尾「进度记录」补一行。
> 计划中途发现假设错误就直接改本文件，不要另开一份。
> 前两份计划见 `REFACTOR_PLAN.md`、`AI_MERGE_PLAN.md`（均已完成）。

---

## ▶ 当前进度：**Phase 1~5 全部完成**（只剩一条浏览器验证留给用户，见文末）

接手时先跑一遍确认基线：

```bash
npm test                          # 应为 103/103 通过
npm run balance 10000             # 角色强度表（Phase 1 排关卡要用）
node difficulty-check.mjs 6000    # 难度公平性，确认公平线仍 ≈60%
node campaign-check.mjs 3000      # 战役曲线，应单调下降 92% → 40%
git log --oneline -3
```

**动手前必读**：`CLAUDE.md` 的「工作约定」一节，特别是
「战斗规则只改 `combat.js`」「浏览器测试必须静音、测完关标签页」两条。

---

## 背景：问题不是"做得不够好"，是**从来没被设计过**

用户反馈战役模式「前后困难梯度、剧情含量、角色对应」三方面都不好。
实测下来，三个问题有同一个根因：**关卡数据是按剧情顺手写的，
没有任何一环和实际强度挂钩。**

### 实测难度曲线（2026-08-24，每关 3000 局）

玩家 = 随机 2 人 + hard 级操作，敌方 = 关卡阵容 + 关卡 AI 档。
公平线 ≈ 59.8%（先手优势约 10 个百分点，详见 `CLAUDE.md`）。
**数字越高 = 越好打。**

| 关卡 | 敌方阵容 | AI档 | 玩家胜率 | 相对公平线 |
|---|---|---|---|---|
| 第一关 墨境初醒 | 剑士+弓手 | easy | 95.2% | +35.4 |
| 第二关 赤焰试炼 | 狂战士+守卫 | easy | 87.2% | +27.4 |
| 第三关 灵泉守护者 | 牧师+法师 | normal | **40.1%** | −19.7 |
| 第四关 暗影双煞 | 刺客+术士 | normal | 47.7% | −12.1 |
| 第五关 熔岩双将 | 狂战士+剑士 | normal | 59.8% | +0.0 |
| 第六关 灵泉四卫 | 守卫+弓手 | hard | 49.0% | −10.8 |
| 第七关 墨皇近卫 | 术士+牧师 | hard | **23.1%** | −36.7 |
| 最终关 墨皇决战 | 法师+刺客 | hard | **62.5%** | +2.7 |

**曲线是坏的，而且坏得很具体：**

1. **最终 BOSS 是全战役第二好打的关（62.5%），比第三关（40.1%）轻松得多。**
2. **真正最难的是第七关（23.1%）**，通关的人多半卡在这里，然后发现最终关反而是送。
3. **第 2→3 关有 47 个百分点的断崖**，前两关白给、第三关直接劝退。
4. 第五关（59.8%）比第三、四关都简单，中段是锯齿不是斜坡。

### 根因：难度完全由"阵容碰巧多强"决定

对照 `npm run balance` 的角色强度（10000 局胜率）：

```
牧师 63.4 > 术士 54.7 > 弓手 54.6 > 守卫 53.5 > 法师 46.0 > 剑士 45.2 > 刺客 43.6 > 狂战士 38.9
```

把每关敌方两人的胜率相加当"阵容强度"，和实测难度几乎完全吻合：

- 第七关 = 术士 54.7 + 牧师 63.4 = **118.1（全游戏最强组合）** → 23.1%
- 最终关 = 法师 46.0 + 刺客 43.6 = **89.6（接近最弱组合）** → 62.5%

**没人算过这笔账。** 剧情里"墨皇最强的两名战士"随手写成了法师+刺客，
而它们恰好是全场最弱的两个角色。

### 根因二：难度杠杆只用了一半

`battle.js` 的属性加成只在 `mode==='ai'` 时应用：

```js
if(gameState.mode==='ai'){
  gameState.p2Units.forEach(u=>applyDifficulty(u, gameState.difficulty));
}
```

**战役模式走不到这里**，所以 `stage.difficulty` 只影响 AI 决策档位、
不影响属性。而 AI 决策档位只有 3 档，且 normal 与 hard 差距很小
（64.2% vs 61.4%，只差 3 个百分点）。

**结果：8 个关卡只能落在 3 个台阶上，而且最高那两个台阶几乎一样高。**
想要平滑的 8 级曲线，光靠 `difficulty` 字段做不到。

### 根因三：剧情和玩法零耦合

- 剧情文本是**纯装饰**：把 `intro`/`outro` 全删掉，玩法一点不变。
- **敌人没有身份**：第五关剧情说"墨皇的两名先锋将领"，实际就是通用的
  狂战士+剑士，和第一关的"流浪剑士"是同一个单位、同一个名字。
- **最终 BOSS 不是 BOSS**：墨皇在剧情里现身，但战斗里根本没有"墨皇"这个单位，
  只有一个普通法师。
- **玩家阵容毫无连续性**：每关重新选 2 人，可以每关换一套。
  更离谱的是**可以选和敌人完全相同的角色**（`renderCharGrid` 不排除 `p2Picks`），
  于是"墨皇的近卫"可能正是你自己带的那两个。
- 剧情文本量也确实少：每关 intro 约 2 行、outro 约 2 行，8 关合计不到 40 行。

### 顺带发现的既有 bug（Phase 5 修）

- `showCampaignComplete()` 显示的「最终战役统计」用的是 `gameState.stats`，
  那是**最后一关单场**的数据，不是整个战役的累计。名不副实。
- **战役赢了没有战斗结算界面**：`showResult` 在 campaign 分支直接跳 cutscene，
  打完一关看不到任何伤害/MVP 统计（人机模式有）。
- 第六关标题叫「灵泉四卫」，实际只有 2 个敌人。

---

## 目标

1. **难度曲线单调、平滑、可测量。** 第一关轻松上手，最终关最难，中间无断崖无回落。
2. **敌人有身份。** 剧情里提到的人，就是你打的那个单位（有名字、有立场）。
3. **最终 BOSS 是真 BOSS。** 墨皇要有自己的属性和机制，不是换皮法师。
4. **剧情有量、有节奏。** 分段播放，战前战后都有对话，而不是一次糊一整段。
5. **角色和剧情绑定。** 玩家阵容在战役里有来历、有成长，不是每关随便选。

## 非目标（避免范围膨胀）

- 不做分支剧情 / 多结局。线性 8 关讲好一个故事就够。
- 不做新角色的完整实现（新画法/新技能树）。BOSS 用现有角色数据改属性 + 换名字 + 加被动。
- 不引入任何 npm 依赖，不引入构建步骤。
- 不改 `combat.js` 的战斗公式本身（BOSS 的强度用属性和现有被动实现）。
- 不动人机对战 / PVP / 平衡测试三个模式的现有行为。

---

## Phase 1 — 让战役难度可测量，并重排曲线

**先有尺子再调数值**，这是本项目反复验证过的顺序（见 `difficulty-check.mjs`）。

- [x] 新建 `campaign-check.mjs`，输出上面那张表（每关阵容 / AI档 / 玩家胜率 / 相对公平线）。
      直接复用 `sim.js` 的 `simOneBattle(p1ids, p2ids, scene, {p1Ai,p2Ai,p2Mod})`，
      **不要另抄一份战斗循环**。参考 `difficulty-check.mjs` 的写法。
- [x] `campaign.js` 每关新增 `enemyMod` 字段（形如 `{atk:0.9}` / `{atk:1.1, hp:1.15}`），
      作为难度微调旋钮。**理由：只靠 `difficulty` 的 3 档 AI 无法拉出 8 级曲线**
      （normal 与 hard 只差 3 个百分点）。
      属性加成是更连续的杠杆——实测攻击 +15% 值 −13.8 个百分点，
      是全部杠杆里最有力的一个（见 `CLAUDE.md` 难度减半那条）。
- [x] `combat.js` 新增 `applyStageMod(unit, mod)`（和 `DIFFICULTY_MODS` 放一起），
      `battle.js` 在 campaign 分支调用它。
      **注意**：campaign 现在完全不吃 `applyDifficulty`，这次是新增一条 campaign 专用路径，
      不要顺手把 `mode==='ai'` 改成包含 campaign——那会让所有关卡的难度整体跳变，
      和 `enemyMod` 打架。
- [x] 按角色强度表重排 8 关的敌方阵容，让「阵容强度」单调上升。
      强度 = 两名角色在 `npm run balance` 里的胜率之和。参考值：
      最弱 狂战士+刺客 82.5，最强 术士+牧师 118.1。
- [x] 用 `campaign-check.mjs` 反复迭代，逼近目标曲线：

      | 关卡 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
      |---|---|---|---|---|---|---|---|---|
      | 目标玩家胜率 | 92% | 85% | 78% | 72% | 65% | 58% | 50% | 42% |

      允许 ±4 个百分点误差。**关键是单调下降 + 最终关最难**，绝对值可以整体平移。
- [x] 把最终曲线记进 `CLAUDE.md`

**验证**：`campaign-check.mjs` 输出单调下降；`npm test` 全绿；
浏览器实打第 1、3、8 关各一局确认手感（静音、测完关标签页）。

**注意**：`campaign-check.mjs` 里的"玩家"是 hard 级 AI，是**上限**。
真人会比这低，所以目标曲线的最终关定在 42% 而不是更低。

## Phase 2 — 敌人有身份，BOSS 是真 BOSS

- [x] `campaign.js` 每关的 `enemy` 从 `['swordsman','archer']` 扩展为带身份的对象：
      `[{id:'swordsman', name:'流浪剑士·墨白'}, {id:'archer', name:'游猎者·苍'}]`
      （保持向后兼容：也接受纯字符串，缺省用角色原名）
- [x] `combat.js` 的 `createUnit(charId, player, slot, override)` 支持 `override`：
      `{name, atk, hp, def, passive}`。BOSS 就是靠它实现，不新增角色数据。
- [x] `battle.js` 建 p2 单位时把身份传进去；确认战斗日志、单位名牌、
      结算界面显示的都是剧情名而不是"法师"。
- [x] 最终关加真正的墨皇：单人或双人，属性显著高于普通角色，
      建议复用现有被动（如 `corruptBonus` / `reflect`）而不是写新机制。
      **强度用 `campaign-check.mjs` 校准到目标曲线的 42%，不要凭感觉给数值。**
- [x] 顺手修：第六关标题「灵泉四卫」改成和实际敌人数一致的名字。

**验证**：浏览器打最终关，确认名牌显示"墨皇"、属性明显更强、战斗不报错。

## Phase 3 — 剧情扩写 + 分段播放

- [x] `main.js` 的 `showCutscene(stageTitle, text, btnLabel, callback)` 改造成
      支持**多段文本数组**，点击逐段推进，最后一段才触发 callback。
      现在是一次性 `textContent = text` 糊一整段。
- [x] `campaign.js` 的 `intro`/`outro` 从字符串改成字符串数组（分段）。
      保持向后兼容：字符串自动包成单元素数组。
- [x] 扩写文本：每关 intro 3~5 段、outro 2~3 段。
      **写作要求**：敌人要用 Phase 2 给的身份名说话，让"你打的人"和"说话的人"是同一个。
- [x] 加一条贯穿的主线（墨皇为何统治墨境 / 主角为何而来），
      在 8 关里分段揭示，而不是每关各讲各的。
- [~] 打字机效果——**决定不做**。分段推进本身已经是节奏控制，再加打字机会让「点一下」
      产生两种含义（补完这段 vs 翻到下一段），把最简单的交互复杂化。计划里本就标了「可选」。

**验证**：浏览器走完第 1 关的完整 intro→战斗→outro 流程，分段推进正常。

## Phase 4 — 角色与剧情绑定（本计划的核心诉求）

**主张：采用"固定主角 + 逐步解锁队友"，不做全招募制。**

理由：全招募制（打赢就把敌人收编）和现有剧情文本冲突（现在写的是敌人"倒下""阵亡"），
且要重做整个选角流程。固定主角方案能达成同样的"角色-剧情对应"，工作量小得多，
而且天然解决"玩家可以选和敌人相同角色"的荒谬感。

- [x] 战役有固定主角（建议剑士，剧情名"墨白"），每关**必选**，不可替换。
      剧情全部围绕他写。
- [x] 队友池随进度解锁：第 1 关只有 1~2 个可选队友，每通一关解锁 1 个。
      解锁事件写进 outro 剧情（"XX 决定与你同行"）。
- [x] `renderCharGrid` 在 campaign 模式下：主角锁定选中且不可取消；
      未解锁的队友显示为锁定态；**排除本关敌方正在使用的角色**。
- [~] 解锁进度**不另存 localStorage**——直接从 `inkfight_campaign`（已通关数）推。
      计划原本建议加一个 `inkfight_campaign_roster`，但那就是同一份知识两份实现，
      迟早对不上；`resetCampaign()` 也因此不用多清一个 key。原文：
      建议 `inkfight_campaign_roster`）；`resetCampaign()` 要一并清掉。
- [x] 队友解锁顺序要和 Phase 1 的难度曲线对齐：
      **解锁强角色会让后续关卡变简单**，所以解锁一个强队友之后，
      下一关的 `enemyMod` 要相应上调。重跑 `campaign-check.mjs` 校准
      （校准时玩家阵容要用"该关实际可用的池子"，不是随机 2 人）。

**验证**：`campaign-check.mjs` 改用"每关实际可选阵容"重算，曲线仍单调；
浏览器从第 1 关连打 3 关，确认解锁流程和锁定态正确。

**若嫌 Phase 4 太大可以只做前两条**（固定主角 + 排除敌方角色），
剩下的解锁机制留到以后——前两条已经能解决"角色和剧情不对应"的主要观感。

## Phase 5 — 收尾与既有 bug

- [x] 修「最终战役统计」：现在显示的是最后一关单场数据。
      需要跨关累计（存在 `gameState.campaignTotals` 或 localStorage）。
- [x] 战役打赢一关后加战斗结算（现在直接跳 cutscene，看不到伤害/MVP）。
      建议：结算界面加一个"继续剧情"按钮，而不是跳过结算。
- [x] 更新 `CLAUDE.md`：模块表补 `campaign-check.mjs`，
      记录新的难度曲线和战役设计约定。
- [~] 浏览器完整通关一遍（8 关）——**只做到一半，剩下的留给用户**。原因见下方进度记录。
      已验证：第 1 关完整链路（4 段 intro → 战斗 → 结算 → 继续剧情 → 3 段 outro
      含解锁公告 → 第 2 关选角池正确刷新）、最终关墨皇独战（渲染 / 每回合出手 /
      整套术士机制 / 守卫反弹被动）、战败链路、各关属性加成与模拟器逐一对齐。
      未验证：**通关总账界面（战役总战绩）的渲染结果**，
      全程静音、测完关标签页。

---

## 工作量与中断策略

- **Phase 1** 中（~1.5 小时）。纯数据 + 少量代码，风险低，**立竿见影**，
  单独做完就已经解决了用户抱怨的第一个问题。
- **Phase 2** 中（~1 小时）。`createUnit` 加 override 是关键改动，要小心别影响其他模式。
- **Phase 3** 中偏大（~1.5 小时）。代码改动小，主要是写文本。
- **Phase 4** 大（~2 小时）。改选角流程，涉及 UI 状态。可只做前两条。
- **Phase 5** 小（~40 分钟）。

**每个 Phase 结束时代码都应处于可提交、测试全绿的状态。**
若中途 context 重置，从本文件未勾选的第一项继续即可。

**建议顺序**：Phase 1 → 2 → 5(前两条 bug) → 3 → 4。
把 Phase 5 的 bug 修提前，是因为它们独立、很小、且修完战役体验立刻完整。

## 风险与应对

- **风险：Phase 4 解锁机制会让 Phase 1 校准好的曲线失效。**
  应对：Phase 4 最后一条明确要求重跑校准。**不要跳过**——解锁一个牧师
  （全场最强，63.4%）对后续关卡难度的影响，比任何 `enemyMod` 都大。
- **风险：`createUnit` 加 override 影响其他模式。**
  应对：override 缺省为 `undefined`，走原路径；`npm test` 里 `combat.test.js`
  已覆盖 `createUnit`，改完必须跑。
- **风险：剧情写太多导致玩家跳过。**
  应对：分段播放本身就是节奏控制；每段控制在 2~3 行，允许连点快速推进。
- **风险：为了曲线好看而让最终关变得枯燥**（比如单纯堆属性）。
  应对：BOSS 的难度优先来自"阵容强度 + AI 是 hard"，`enemyMod` 只做最后微调，
  建议 atk 倍率不超过 1.25。

## 参考：本项目已验证的经验（别重新踩）

- **先有尺子再调数值。** `difficulty-check.mjs` 的价值在于它先量出公平线（59.8%）
  再报相对偏差；拿 50% 当基准会把每一档都误判成偏难。
- **属性加成里，攻击是最强杠杆**（+15% 攻 = −13.8 个百分点，
  而 +20% 回蓝只有 −2.3）。要大幅调难度就动攻击，要微调就动回蓝。
- **AI 决策档位拉不开差距**（normal 64.2% vs hard 61.4%）。别指望靠切 AI 档做曲线。
- **同一份知识不要两份实现。** 本项目已经因此出过三次 bug。
  `enemyMod` 的数值只应存在于 `campaign.js`，`campaign-check.mjs` 必须读同一份。
- **浏览器实测能抓到单测抓不到的东西**（`AI_MERGE_PLAN.md` Phase 5 抓到的空目标崩溃）。
  每个 Phase 都要真跑一局。

## 进度记录

- 2026-08-24：完成现状调研，实测 8 关难度曲线（见上表），
  定位三个根因：(1) 关卡阵容按剧情随手写，难度实际由阵容强度决定，
  最终 BOSS 用了全场最弱的两个角色（62.5%，比第三关的 40.1% 还好打），
  真正最难的是第七关（23.1%，用了全场最强组合）；
  (2) campaign 模式不吃 `applyDifficulty`，难度杠杆只剩 3 档 AI，
  而 normal 与 hard 只差 3 个百分点，8 关挤在 3 个台阶上；
  (3) 剧情纯装饰，敌人无身份，墨皇不存在于战斗中，玩家阵容每关可随意更换
  （甚至能选和敌人一样的角色）。写下本计划。

- 2026-08-24：**Phase 1 完成**。新建 `campaign-check.mjs`（尺子）、
  `combat.js` 新增 `applyStageMod`（旋钮，走 campaign 专用路径，不与
  `applyDifficulty` 合并）、`campaign.js` 每关加 `enemyMod`、8 关阵容按实测强度重排
  （每个角色恰好出场两次）。补 5 条单测（91 → 96 全绿）。

  **最终曲线（5000 局/关，公平线 60.8%）**：

  | 关卡 | 敌方阵容 | AI档 | enemyMod | 玩家胜率 | 目标 | 偏差 |
  |---|---|---|---|---|---|---|
  | 1 墨境初醒 | 狂战士+刺客 | easy | hp×1.10 | 92.4% | 92% | +0.4 |
  | 2 赤焰试炼 | 剑士+法师 | normal | hp×0.90 | 83.8% | 85% | −1.2 |
  | 3 灵泉伏击 | 刺客+剑士 | normal | hp×1.05 | 75.9% | 78% | −2.1 |
  | 4 虚空关隘 | 守卫+弓手 | normal | — | 73.4% | 72% | +1.4 |
  | 5 熔岩双将 | 弓手+术士 | normal | — | 64.0% | 65% | −1.0 |
  | 6 灵泉防线 | 法师+守卫 | hard | — | 57.1% | 58% | −0.9 |
  | 7 墨皇近卫 | 牧师+狂战士 | hard | hp×1.10 | 47.9% | 50% | −2.1 |
  | 8 墨皇决战 | 术士+牧师 | hard | hp×1.05 | 39.5% | 42% | −2.5 |

  **三个推翻原有假设的实测发现**（细节见 `CLAUDE.md`）：
  1. 「两名敌人胜率之和」预测不了关卡难度，真正的驱动是**队伍续航**
     （狂战士+守卫 和 93.5 → 41.9%，比 守卫+牧师 和 116.7 → 52.0% 还难打）。
  2. **场景 buff 是很强的难度杠杆**：同一阵容在虚空 26.2%、在熔岩 47.7%
     （+15% 伤害破续航）。校准必须锁定该关自己的场景，用随机场景会差 10 个百分点。
  3. **旋钮选 atk 还是 hp 要看敌方靠什么赢**：第 8 关对 atk 几乎免疫、对 hp 极敏感。
  4. **简单 AI 拉不出台阶**（任何阵容都 90%+），所以只有第 1 关用 easy。

  浏览器实测第 1/3/8 关，敌方血量与模拟器完全一致（154/105、100/131、105/113），
  战斗流程无报错。

  **留给 Phase 2 的坑**：第 8 关现在是 `hp×1.05` 的普通术士+牧师。
  加入墨皇 override 后属性会变，**必须重跑 `campaign-check.mjs` 重新校准到 42%**。

- 2026-08-25：**Phase 2 完成**。敌人有身份了，墨皇是真 BOSS。
  - `combat.js` 的 `createUnit(charId, player, slot, override)` 加了第四个参数
    （可覆盖 name/color/hp/sp/atk/def/crit/dodge/spRegen/passive），另加 `unitSpec(entry)`
    把「字符串 or {id,name,...} 对象」这件事收敛成一处，`battle.js` 和 `sim.js` 共用。
    缺省 undefined 走原路径，其它模式行为不变（单测锁住）。
  - `campaign.js` 16 个敌人全部有了剧情名（荒野狂徒·赤牙、炉守·铁衣、伏影·青蝉、
    关隘守将·磐、先锋将·断雁、守法者·澜、近卫祭司·白鸦……）。
  - `main.js` 拆成两个字段：`p2Picks` 仍是纯 id 数组（雷达图那边按 id 读），
    新增 `p2Roster` 存带身份的原始条目，`battle.js` 建单位时用后者。
  - **最终关改成墨皇独战**（术士底子，260/22/8，被动沿用「腐化侵蚀」）。
    理由是实测出来的机制事实：**本作回合流程是「双方各行动一个单位」，
    队伍人数不影响行动次数，只影响血池和技能池。**
    所以两人队里 BOSS 只能隔回合出手一次，单人则每回合都出手——
    「165 血的换皮术士」和「每回合都放大招的 260 血墨皇」，后者才叫 BOSS。
  - 校准过程：165/19/7 + 圣女 → 6.1%（远超目标）。血量预算极紧，
    +40% 血就吃掉 24 个百分点。最后 260/22/8 独战 = 40.7%，落在目标 42% 的 −1.3。
  - 补 7 条单测（96 → 103 全绿）。
  - 浏览器实测：最终关单人 BOSS 渲染正常（HP 260/260、SP 130/130），
    墨皇每回合出手，瘟疫 / 灵魂收割 / 暗影弹整套术士机制可用，
    守卫的「铁甲反弹」对他照常生效；普通关名牌与日志显示的都是剧情名，
    `enemyMod` 叠在 override 之上仍然正确（狂徒 140×1.1=154）。

  **当前曲线（5000 局/关，公平线 59.2%）**：
  92.5 → 84.0 → 76.2 → 73.5 → 63.2 → 56.2 → 46.8 → **40.7**（单调，最大偏差 3.2）。

  **留给 Phase 3 的坑**：第 8 关的圣女·素雪已经不是战斗单位了，
  但她仍是个可用的剧情角色（可在过场里出现），写文本时注意别写成「她在场战斗」。

- 2026-08-25：**Phase 4 完成**（角色与剧情绑定，本计划的核心诉求）。
  - 固定主角：`CAMPAIGN_HERO` = 剑士，剧情名「墨白」，每关预选且不可取消。
  - 队友池 `CAMPAIGN_ALLIES` 7 人，各有剧情名（铁山/青梧/野火/无声/白羽/墨蛇/素问），
    第 1 关给前两个，之后每通一关解锁一个，解锁公告接在 outro 末尾。
    顺序按「弱的先给」排，牧师（全场最强）放最后。
  - 选角界面四种状态：主角锁定（金边）/ 可选 / **本关敌方**（禁选）/ 未解锁（？？？）。
    「能选到和敌人一模一样的角色」这个最出戏的问题解决了。
  - 玩家单位也走 `p1Roster` + `createUnit` override，战斗日志、名牌、结算、
    跨关战绩里显示的全是剧情名。
  - **与计划的一处偏离**：解锁进度没有另存 `inkfight_campaign_roster`，
    而是直接从 `inkfight_campaign`（已通关数）推。理由是同一份知识两份实现迟早对不上。

  **重新校准后的曲线**（5000 局/关，玩家 = 墨白 + 该关实际可选队友轮换，公平线 60.3%）：

  | 关卡 | enemyMod | 玩家胜率 | 目标 | 偏差 |
  |---|---|---|---|---|
  | 1 墨境初醒 | hp×0.95 | 93.7% | 92% | +1.7 |
  | 2 赤焰试炼 | hp×1.30 | 86.0% | 85% | +1.0 |
  | 3 灵泉伏击 | hp×1.05 | 78.8% | 78% | +0.8 |
  | 4 虚空关隘 | hp×0.84 | 74.2% | 72% | +2.2 |
  | 5 熔岩双将 | hp×1.10 | 65.8% | 65% | +0.8 |
  | 6 灵泉防线 | hp×0.85 | 59.0% | 58% | +1.0 |
  | 7 墨皇近卫 | hp×0.95 | 50.1% | 50% | +0.1 |
  | 8 墨皇决战 | hp×0.91 | 41.6% | 42% | −0.4 |

  单调下降，最大偏差 2.2——比 Phase 1 那版还准。

  **过程中量出来的两件事，值得记住**：
  1. **这个游戏极度吃阵容克制，而且到处都是，不只是 BOSS 关。**
     普通的第 6 关，玩家 28 种阵容的胜率标准差就有 26.5、极差 87；
     单人墨皇是 31.5 / 97，只是略高而已。
     `npm run balance` 对随机阵容取平均，把这件事整个掩盖掉了。
     所以 `campaign-check.mjs` 现在会**逐个队友报胜率**，只看均值会漏掉「带错人必输」。
  2. **固定主角让最差情况好转了**：Phase 2 时随机 2 人打墨皇，最差组合只有 0.2%；
     现在主角固定成剑士（提供稳定的伤害下限）+ 排除敌方角色，最差的队友也有 12%。

  **留给 Phase 5 的**：更新 `CLAUDE.md`、浏览器完整通关一遍。

- 2026-08-25：**Phase 5 完成，五个 Phase 全部做完。**
  - 修「最终战役统计」：以前显示的是最后一关单场数据，现在按关卡 id 存每关一份
    （`inkfight_campaign_totals`）、展示时求和。按 id 存而不是直接累加，
    是因为通关的关卡可以重打，重打只该覆盖那一关。`resetCampaign()` 一并清掉。
  - 战役赢下一关现在也走完整结算界面，看完点「继续剧情 →」再进过场。
    进度和统计在赢下的**当场**就写，不等玩家点按钮——中途关掉页面不该丢进度。
  - `CLAUDE.md` 已更新：模块表补了 `campaign-check.mjs`、`campaign.js` 那行补了
    主角/队友解锁，并记了三条实测结论（阵容克制、场景是杠杆、旋钮选 atk 还是 hp）。

  **浏览器实测中抓到两个既有 bug（都是单测抓不到的）**：
  1. 结算界面的数字滚动动画用 `isNaN(parseInt(v))` 判断该不该滚，于是
     「22（守卫）」这种也被当数字滚了一遍，滚完括号里的名字就没了——
     **「最高单次伤害」那一行一直没显示过是谁打的。** 改成只滚纯数字。
  2. **`showResult` 会被调用两次。** `checkVictory()` 有两个调用点
     （`nextTurn` 和「行动单位已阵亡」那条分支），胜负已定时两边**各排一个**
     `setTimeout(showResult, 700)`。正常速度下第二次在玩家还没点按钮时就跑完了，
     看不出来；但玩家只要在 700ms 内点掉「继续剧情」，延迟的第二次就会把他
     **从过场/通关界面拽回战斗结算界面**。加了 `gameState.resultShown` 幂等标记。
     这个 bug 是被浏览器降频放大之后才现形的。

  **没做完的那一条：完整通关 8 关。**
  Claude Code 的内置浏览器面板在本会话里无法显示，标签页一直处于隐藏状态，
  Chrome 对长时间隐藏的标签启用「重度降频」——游戏的 `setTimeout` 链被压到
  **约每分钟一个**，一场战斗要跑半小时。刚重载后能快一阵，但很快又被压回去。
  最终关另外试了 5 次都没打赢（模拟器说带白羽有 68%），因为驱动脚本只会挑
  标称伤害最高的技能，不会管蓝、不会留眩晕。
  **所以「战役总战绩」界面的渲染结果没有亲眼确认过**——它读的那份数据
  （每关一条记录）已经验证写入正确，渲染代码也就十几行，但没实测就是没实测。
  建议用户在自己的浏览器里（不降频）打通一遍确认。

  **另外值得用户留意的一件事**：最终关对「随便点最大伤害」这种打法惩罚极重。
  模拟器给的 41.6% 是**接近最优操作**的胜率；naive 打法 5 次全败（而且有两次
  把墨皇打到只剩 25 / 34 血）。如果实际玩起来觉得太劝退，
  把第 8 关的 `enemyMod` 从 `{hp:0.91}` 往下调即可，改完跑 `node campaign-check.mjs`。
