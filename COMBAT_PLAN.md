# 战斗深度重做 · 计划

> 持久化执行计划，供 context 重置后的新对话继续。
> 每完成一条把 `[ ]` 改成 `[x]`，并在末尾「进度记录」补一行。
> **每条任务都是自包含的**：只读本文件 + 对应代码就能动手。
> 前五份计划见 `REFACTOR_PLAN.md` / `AI_MERGE_PLAN.md` / `CAMPAIGN_PLAN.md` /
> `ENGINE_PLAN.md` / `DIFFICULTY_PLAN.md`（均已完成）。

---

## ▶ 当前进度：**方案已确认，代码未动**

用户确认的范围：**先只写设计文档**。边界：**8 个角色的身份和定位是红线**
（剑士/法师/守卫/刺客/牧师/狂战士/弓手/术士，人设与大致定位不变，
但**技能可以重配**）。剧情、难度四档、技能数值均可推翻。

接手时先跑一遍确认基线：

```bash
npm test                     # 应为 189/189 通过
node depth-check.mjs 2000    # 策略价值，见下方基线
node choice-check.mjs 1500   # 主流技能占比，见下方基线
```

**动手前必读**：`CLAUDE.md` 的「工作约定」一节。

---

## 起因

2026-08-26，用户反馈：

> 「其实策略性不够，所以打敌人的时候会不会输纯看比如什么负面有没有上之类的。
> 反正墨皇肯定是纯看运气了。」

实测确认用户是对的，而且**根因不是数值没调好，是这个战斗系统里没有决策**。

---

## 诊断：三个实测数字

为此新增两个诊断脚本（`depth-check.mjs` / `choice-check.mjs`），
它们同时也是本计划的**验收标尺**。

### 数字一：玩家的全部技术只值 12.6 个百分点

把玩家替身的决策水平从「完美」一路降到「闭眼乱按」（`depth-check.mjs`，2000 局/格）：

| 玩家水平 | 完美 | 熟手 | 一般 | 生手 | 乱按 | 策略价值 |
|---|---|---|---|---|---|---|
| 随机阵容 · 困难档 | 60.1% | 53.4% | 47.5% | 40.5% | 14.5% | 45.5 |
| 随机阵容 · 墨皇档 | 51.4% | 47.8% | 40.4% | 33.7% | 9.7% | 41.7 |
| 战役第 8 关 · 墨皇 | 61.5% | 55.9% | 47.0% | 39.7% | 6.2% | 55.3 |

**关键不是总落差 45.5，是它的分布**：
「乱按 → 生手」涨 26 点，「生手 → 完美」只涨 20 点。
也就是说**只要玩家不做蠢事，剩下的思考几乎没有回报**。
完美与一般之间只差 **12.6 个百分点**——这是「想得更清楚」的收益上限。

### 数字二：一个玩家碰不到的骰子值 29.6 个百分点

眩晕命中率强行钉死后（`stun-check.mjs`，4000 局/格，p1 固定带眩晕角色）：

| 眩晕全部落空 | 实际命中率 | 眩晕必中 |
|---|---|---|
| 58.3% | 61.9% | 87.9% |

**落差 29.6 点，全部由骰子掌管，玩家没有任何操作可以影响它。**
对照上面的 12.6——**骰子的话语权是玩家技术的 2.3 倍**。
用户说的「纯看负面有没有上」，就是这个。

根因是本作特有的回合结构：**「双方各行动一个单位」**，
所以一次眩晕不是「减速」，是**直接偷走对方一整个行动**。
在别的游戏里眩晕是个中等强度的控制，在这里它是全场最强的单一效果。

### 数字三：58.4% 的回合在放同一个技能

完美玩家的技能使用分布（`choice-check.mjs`，1500 局 / 33405 个决策点）：

| 角色 | 主流技能 | 占比 | 分布 |
|---|---|---|---|
| 剑士 | 破甲突刺 | 51% | 斩击 22 / 旋风斩 20 / 剑气 7 / 破甲突刺 51 |
| 法师 | 墨之洪流 | 69% | 墨弹 6 / 墨之洪流 69 / 灵能过载 22 / 冥想 3 |
| 守卫 | 盾击 | 41% | 盾击 41 / 铁壁 18 / 嘲讽 11 / 盾墙反击 30 |
| 刺客 | 暗影突袭 | **82%** | 匕首 10 / 暗影突袭 82 / 毒刃 8 / 消失 **0** |
| 牧师 | 治愈之光 | 61% | 光击 20 / 治愈之光 61 / 净化 12 / 祝福 7 |
| 狂战士 | 鲜血之力 | 60% | 重击 39 / 狂暴 **1** / 鲜血之力 60 / 不屈 **0** |
| 弓手 | 束缚箭 | 47% | 射击 7 / 穿透箭 34 / 集中 12 / 束缚箭 47 |
| 术士 | 瘟疫 | 56% | 暗影弹 2 / 腐化爆发 30 / 瘟疫 56 / 灵魂收割 12 |

- 平均主流技能占比 **58.4%**（健康区间 < 40%）
- 无悬念回合（最优甩开次优 30%+）**59.2%**（健康区间 < 50%）
- 其中「只有一个技能放得起」只占 11.6%，
  所以**不是没蓝，是最优解太明显**

**32 个技能里有 8 个使用率低于 10%**（消失 0 / 不屈 0 / 狂暴 1 / 暗影弹 2 /
冥想 3 / 射击 7 / 剑气 7 / 祝福 7）。四分之一的内容是死的。

最优打法可以概括成一句话：**有蓝放大招，没蓝平A**。

### 排除的嫌疑：暴击和闪避

把 crit / dodge 全部归零重跑，策略价值从 45.5 只变到 46.2。
**问题不在暴击闪避**，它们只是背景噪声。真正的随机大头是眩晕。

### 墨皇为什么是纯运气

他只是「术士 + 260血 + 22攻」，没有阶段、没有机制、没有可破解的弱点。
玩家能做的判断和打普通术士完全一样，只是数字更大。
所谓「难」= 容错更低 = **同一批骰子的权重更高**。

---

## 别人怎么做的

三条，都指向同一个病根。

### 1. 输入随机 vs 输出随机

Sid Meier 那一派的核心区分：

- **输入随机**——发生在玩家决策**之前**（地图生成、抽牌、场景 buff）。
  它创造新局面，逼玩家针对当前手牌重新想办法，**放大**决策的价值。
- **输出随机**——发生在玩家决策**之后**（命中率、暴击、眩晕判定）。
  它在决策和结果之间插入噪声，**稀释**决策的价值。

> 「一般来说，有经验的玩家对输入随机的接受度远高于输出随机，
> 并且输入随机会让玩家更多地使用技巧而不是运气。」

**本作的随机全部是输出随机。** 场景 buff 算是唯一的输入随机，
但它是全局对称的，等于没有。

### 2. 完美信息 / 意图公开

*Into the Breach* 和 *Slay the Spire* 的杀手锏：**敌人下一步要干什么，
明明白白写在头上**。Subset Games 的设计目标原话是——他们要让
**每一次失败都是玩家自己的错**。

> 「这股持续不断的完美信息流让玩家更少地花时间搞懂游戏怎么运作，
> 更多地花时间琢磨怎么赢。」

一旦意图公开，每个回合立刻变成一道题：该开盾还是该抢杀？
该换人挡刀还是赌他打不死？**本作现在完全看不到敌人要干嘛，
所以玩家只能反应，不能计划。这是策略性缺失最大的单一原因。**

### 3. 避免支配性策略

有深度的回合制要求三件事同时成立：**选项多样**、**取舍真实**
（攻 vs 守、眼前 vs 长远）、**情境足够变化以致没有可以无脑重复的最优解**。

本作前两条勉强成立，第三条完全不成立——最优解不随情境变化（见数字三）。

来源：
- [Randomness and Game Design — Game Developer](https://www.gamedeveloper.com/design/randomness-and-game-design)
- [The 2 types of randomness — Board Game Design Course](https://boardgamedesigncourse.com/the-2-types-of-randomness/)
- [Perfect Information: The Killer Feature of Slay the Spire and Into the Breach](https://jeremiahgames.com/2019/03/04/perfect-information-the-killer-feature-of-slay-the-spire-and-into-the-breach/)
- [Road to the IGF: Subset Games' Into the Breach](https://www.gamedeveloper.com/game-platforms/road-to-the-igf-subset-games-i-into-the-breach-i-)
- [Into the Breach & Enemy Intentions](https://atomicbobomb.home.blog/2020/05/17/into-the-breach-enemy-intentions/)
- [12 ways to improve turn-based RPG combat systems](https://www.gamedeveloper.com/design/12-ways-to-improve-turn-based-rpg-combat-systems)
- [Boss Design: How to Make an Unforgettable Boss Battle](https://gamedesignskills.com/game-design/game-boss-design/)

---

## 设计结论（本计划的四条原则）

1. **玩家永远知道下一击是什么。** 意图公开是地基，不是难度旋钮——
   任何难度都不许藏。难度回到「AI 决策水平 + 属性」两层，和现在一样。
2. **随机可以决定「局面」，不许决定「结算」。** 场景、阵容、出手顺序可以随机；
   命中、暴击、控制不许掷骰。要保留强弱差异就改成**可见的确定性条件**。
3. **每个技能都要有它唯一擅长的局面。** 判据是使用率——低于 5% 的技能
   要么改要么删，不留展示品。
4. **AI 的失误必须是决策失误，不是骰子失误。** 承诺制之后 AI 会执行
   已经不合时宜的行动，这是**特性**（玩家的操作让它落空了），要在 UI 上说清楚。

---

## 边界：什么不能动

**8 个角色的身份和定位是红线。** 技能可以整套重配，但下面这一列必须仍然成立：

| 角色 | 必须保住的身份 | 新机制下大致怎么落地 |
|---|---|---|
| 剑士 | 均衡输出、破甲 | 靠 combo 起手（破防 → 必暴击），没有短板也没有爆点 |
| 法师 | 高爆发 / 控制 | 最高倍率 + 打断，代价是脆和吃 SP |
| 守卫 | 坦克 / 反制 | **意图公开的最大受益者**：嘲讽改写敌人意图，护盾按预告量开 |
| 刺客 | 高伤 / 脆皮 | 处决（对残血必暴击）+ 主动闪避，靠信息躲掉致命一击 |
| 牧师 | 治疗 / 辅助 | 从「事后补血」变成「事前预判」，净化用来废掉敌人 combo |
| 狂战士 | 越残越强 | 血怒保留；冷却制给「狂暴」腾出真正该用的回合 |
| 弓手 | 远程 / 灵活 | AoE + 打断，靠蓄力与冷却做节奏 |
| 术士 | 腐化 / 爆发 | 腐化层是**公开的倒计时**，玩家看得见爆发何时到来 |

剧情、关卡结构、难度四档**不设边界**，但见「风险」一节——它们会被连带影响。

---

## 任务 1 — 敌人意图公开 + 承诺制 ★地基

**这是本计划的核心。任务 2/3/4 的价值都建立在它之上**——
去掉随机、复活防御技能、BOSS 机制，全都需要玩家「看得见」才成立。

### 设计

- 轮到玩家行动时，**先算出敌方下一个行动单位会做什么**，公开显示：
  ```
  墨皇  →  灵魂收割  →  目标：墨白   预计 68 伤害
  ```
  显示位置：敌方单位血条下方，外加一条指向目标的高亮线。
- **承诺制**：AI 在意图公开的那一刻就锁定行动，轮到它时**照做**，
  即使玩家的操作已经让这步棋变臭。玩家于是可以主动让它落空。
- 失效处理（**只重解目标，不重选技能**，否则承诺就是假的）：
  - 目标已死 → 按同类规则重选一个合法目标
  - 单位被打断 → 行动取消，UI 要有明确的「意图被打断」反馈（这就是爽点）
  - HP 不足以支付 `hpCost` → 退化为普攻

### 为什么这一条能同时修好几个问题

- 玩家第一次可以**计划**：抢杀 / 开盾 / 换人挡 / 嘲讽改道 / 预读治疗。
- **防御技能自动复活**。「消失」0%、「不屈」0%、「铁壁」18% 不是因为它们弱，
  是因为**不知道该什么时候开**。信息一给，它们立刻有了正确答案。
- 守卫和牧师的定位从「被动挨打后补救」变成「主动布防」，人设反而更立得住。

### 要动的地方

- [x] **新建 `intent.js`**（而不是塞进 `battle.js`）：`nextActor` / `makeIntent` /
      `resolveIntent` / `estimateDamage` 四个纯函数。理由写在文件头——这是回合流程
      编排不是战斗规则，所以不进 `combat.js`；但 `battle.js` 和后续的 `sim.js`
      必须共用同一份，否则又是「同一份知识两份实现」。
- [x] `battle.js`：`startTurn` 的出手单位选择收敛到 `nextActor`
      （原来那段 `p1LastActed`/`p2LastActed` 判断已删，PvP 首次出手的
      `showUnitPicker` 分支保留）。
- [x] `state.js`：新增 `gameState.enemyIntent`，`startBattle` 里重置。
- [x] `battle.js`：`updateEnemyIntent()` 在玩家回合开始时生成并锁定意图；
      `aiAct` 改为先 `resolveIntent` 兑现承诺，只有 PvP / 没有意图时才现算。
- [x] `battle.js`：`cancelIntentOf()` —— 被眩晕 / 中毒倒下 / 被抢先击杀时
      写一条日志。**这条日志就是玩家操作的回报**，不写玩家只会以为 AI 犯傻。
- [x] `render.js` + `inkfight.html`：意图条 UI（脉冲边框）+ 被瞄准单位的红色内发光。
- [x] `intent.js` 的 `estimateDamage`：`combat.js` 的 `previewDmg` 不看目标，
      这里补上减防和 defDown 增伤——报给玩家的数字不准，公开意图就没意义了。
- [x] `test/intent.test.js`：14 条断言，锁住「技能绝不被换掉」「目标死亡只重解目标」
      「hpCost 不够退化普攻」「AoE 不抛异常」。
- [ ] **`ai-scoring.js` 新增 `opts.incomingThreat`**（见风险 3，这是隐藏工作量大头）：
      让防御类技能在「已知有一记 68 伤害要落在某人头上」时得到正确评分。
      不做这个，`difficulty-check.mjs` 量出来的玩家就是个**无视核心机制的玩家**，
      所有难度数字失真。**任务 1 剩这一条，紧接着做。**

### 验收

- [x] 意图与实际行动 100% 一致（`test/intent.test.js` + 浏览器实测：
      预告「祝福→刺客」→ 执行「祝福 → 刺客」，逐字一致）。
- [ ] `choice-check.mjs` 里「消失 / 不屈 / 铁壁」使用率脱离 0%
      （**要等 `incomingThreat` 做完才测得出来**——现在 sim 侧还没有意图机制）。
- [ ] 手测：能靠嘲讽把预告的攻击引到守卫身上。

### 实测发现（2026-08-26）

**预估伤害对高暴击技能会明显偏低。** 实测预告「暗影突袭→牧师 ≈43」，
实际打出 66（暴击）。43×1.5=64.5，说明**非暴击基线算得是准的**，
差额全部来自暴击——而「暗影突袭」自带 +40% 暴击、刺客本身 18%，
合计 58%，等于这个数字有一多半的时候是错的。

不单独修：**任务 2b 会把暴击改成确定性条件，这个问题届时自动消失。**
在那之前先靠「≈」表达不确定性。**做完 2b 记得回来复验这个数字。**

---

## 任务 2 — 干掉不可控的输出随机

三个随机源，按危害排序处理。

### 2a 眩晕 → 打断（29.6 点的那个）

改成**确定性 + 可见条件**，保留「越蓄力越容易被打断」的原设计意图：

- 眩晕技能**必定生效**，效果从「跳过回合」改为「**取消目标已公开的意图**」。
- 生效条件改成看得见的：**目标 SP ≥ 60% 时打断成立**，否则只造成伤害。
  SP 条玩家一直看得见 → 这是**判断**，不是赌博。
- 防连锁：被打断的单位下一回合获得「不可打断」。否则双眩晕队可以锁死对方。

配合任务 1，这条立刻变成一个真正的取舍：
**取消掉那记 68 伤害的「灵魂收割」，值不值我这回合的 40 点输出？**

- [ ] `combat.js`：`calcStun` / `resolveStun` 重写为确定性；删掉 `basePct`/`spScale`
      的概率语义，改成 `spThreshold`。
- [ ] `data.js`：「灵能过载」「束缚箭」改字段。
- [ ] `ai-scoring.js`：`case 'stun'` 的期望值算法改为确定性判断。
- [ ] `test/combat.test.js`：锁 `calcStun` 的旧断言要重写。
- [ ] `stun-check.mjs` 重跑，**落差应逼近 0**（不再有骰子空间）。

### 2b 暴击 → 可制造的 combo

暴击率归零，改成**满足条件必定暴击**，让暴击从老虎机变成玩家能设计的连招：

| 角色 | 必暴条件（暂定，待实测） |
|---|---|
| 刺客 | 目标 HP < 50%（处决，贴合人设） |
| 剑士 | 目标带「减防」（破甲突刺 → 斩击 的两段连招） |
| 弓手 | 自身带「集中」（把 12% 使用率的死技能救活） |
| 其余 | 无必暴条件，伤害折进基础倍率补偿 |

- [ ] `combat.js`：`calcDamage` 里的 `Math.random()*100 < totalCrit` 改条件判定。
- [ ] `data.js`：角色 `crit` 字段语义改变或移除；基础倍率整体上调补偿期望伤害。
- [ ] `ai-scoring.js`：`dmgOf` 要能算进必暴，否则 AI 不会去做连招。

### 2c 闪避 → 删掉被动，只留主动

`dodge` 5~10% 只贡献噪声，不构成任何决策。

- [ ] 删除单位的被动 `dodge` 属性与 `calcDamage` 里的闪避判定。
- [ ] 保留刺客「消失」（主动、确定生效），它在任务 1 之后本来就会变强。
- [ ] 期望伤害变化要在 `npm run balance` 里复验。

### 验收

- `depth-check.mjs`：**策略价值 45.5 → 目标 > 60**，且「生手 → 完美」这一段
  的涨幅要明显大于现在的 20 点（这一段才是真正的技术回报）。
- `stun-check.mjs` 落差 → 接近 0。

---

## 任务 3 — 消灭「有蓝放大招」

### 3a 给技能加冷却

现在唯一的门槛是 SP，而 SP 每回合自动回 7~12、大招花 20~35，
所以节奏永远是「攒三回合、放一次」，**跟局势无关**。

- [ ] `data.js` 技能加 `cd` 字段；`combat.js` 的 `createUnit` 加 `u.cooldowns`，
      在 `processStartOfTurn` 里递减。
- [ ] `ai-scoring.js` 的 `canUse` 等价物、`ai.js` 的 `canUse` 都要认冷却
      （**这是三处同步点，`CLAUDE.md` 已经警告过这类漏改**）。
- [ ] UI：技能按钮显示冷却回合数。

**冷却顺手修好死技能**：大招进入冷却后会空出回合，
「狂暴」（1%）、「集中」（12%）、「冥想」（3%）这些
「本身不亏、但抢不过大招」的技能，第一次有了它们该出现的位置。

### 3b 收紧 SP，让回蓝技能有意义

降低 `spRegen`，让「冥想」「剑气」从备胎变成节奏的一部分。
具体数值靠 `npm run balance` + `choice-check.mjs` 迭代，本文档不预设。

### 3c 逐个角色复查死技能

任务 1 和 3a 之后重跑 `choice-check.mjs`，对**仍然低于 5%** 的技能逐个处理：
改机制或直接换掉（角色身份是红线，单个技能不是）。

### 验收

- 平均主流技能占比 **58.4% → < 40%**
- 无悬念回合 **59.2% → < 50%**
- **32 个技能全部使用率 > 5%**

---

## 任务 4 — 墨皇从属性怪改成一道题

放在最后：BOSS 必须在新机制稳定之后才能校准。

现在他是「术士 + 260/22/8」，玩家的判断和打普通术士没有区别。

### 设计：三阶段，每阶段换一次机制，每个机制都有解

- **阶段一「书写」**（100%~66%）：常规。每回合公开一个意图，稳定积累腐化层。
  腐化层是**公开的倒计时**——玩家看得见爆发何时到来。
  *解法*：净化 / 打断，在 5 层之前掐掉。
- **阶段二「涂改」**（66%~33%）：每回合行动**两次**，公开两个意图。
  信息量翻倍，玩家要选**救哪一边**。
  *解法*：嘲讽把两记攻击引到守卫身上；或抢在第二次行动前打断。
- **阶段三「重写」**（33%~0%）：每回合**抹掉玩家一个技能 2 回合**（公开抹哪个）。
  逼玩家换打法，正面攻击「有蓝放大招」这个惯性。
  *解法*：技能冷却已经逼出了轮换习惯，这里是对任务 3 的收束。

按 BOSS 设计的通行原则：**阶段之间要有连贯性**，每一段都在为下一段做铺垫，
而且**不做纯血量检查**——三个阶段考的是三种不同的能力。

- [ ] `campaign.js`：墨皇需要超出 `override` 能力的机制，
      要么给关卡加 `bossScript` 字段，要么给 `combat.js` 加阶段钩子。
      **先想清楚放哪，别在 `battle.js` 里就地写规则**（`CLAUDE.md` 头号约定）。
- [ ] `campaign-check.mjs` 重新校准第 8 关到目标 42%。
- [ ] 剧情文本基本可留（写得不错），只改机制。

---

## 任务 5 — 捡回一个白送的决策点

AI 模式下**连「这回合派谁上」都是系统自动决定的**——
`showUnitPicker` 只在 `gameState.mode==='pvp'` 时才调用，
人机和战役模式走的是强制轮换。

- [ ] 让玩家在每轮开始时**选本轮两个单位的出手顺序**。
- [ ] **保留「两个单位轮流」的约束**，只让玩家决定顺序。
      放开成自由选择会立刻产生新的支配性策略（一直派最强的那个）。

配合任务 1 这是个很好的决策：**看到预告之后，选谁去接这一下。**
成本近乎为零，`showUnitPicker` 已经写好了。

---

## 任务 6 — 站位系统（**暂不做**）

前排 / 后排是战术游戏最大的深度来源，但要动 `combat.js`、`battle.js`、
`ai-scoring.js` 和整个渲染层，等于重写战斗。

**触发条件**：任务 1~5 全部完成、验收数字达标之后，
如果 `choice-check.mjs` 的主流技能占比仍然压不到 40% 以下，再考虑开这一条。

---

## 建议顺序与理由

```
任务 1（地基）→ 任务 2 → 任务 3 → 任务 5 → 任务 4 → 任务 0 的重校
```

任务 0 的**代码修复**已经先做了（它是个真 bug，不修的话后面每一个数字都不可信），
但**重校放在最后**——任务 2/3 会把数值全部推翻，现在校等于白校。

- **1 必须最先**：2/3/4 的价值全部依赖「玩家看得见」。
  先做 2 会得到一个「既没有随机、也没有信息」的游戏，比现在更无聊。
- **2 在 3 之前**：先把噪声去掉，`choice-check.mjs` 的读数才干净。
- **5 很便宜**，随时可以插进来。
- **4 最后**：BOSS 只能在新机制稳定后校准。

**每一条任务做完都要跑全套**：

```bash
npm test
node depth-check.mjs 2000
node choice-check.mjs 1500
npm run balance 6000
node difficulty-check.mjs 4000
node campaign-check.mjs 3000
```

---

## 风险与应对

1. **意图公开会让游戏整体变简单。**
   玩家信息量大幅上升，胜率会普涨。目标曲线（简单 85 / 普通 65 / 困难 50 /
   墨皇 35）不变，但 `DIFFICULTY_MODS` 和 8 关的 `enemyMod` **全部要重校**。
   这是预期内的，不是 bug。

2. **承诺制会让 AI 看起来变笨。**
   它会执行已经不合时宜的行动。*Into the Breach* 同理，这是设计的一部分。
   但 UI 必须把「你的操作让它落空了」表达清楚，否则玩家只会觉得 AI 傻。
   现成的 `hesitated` 机制（`DIFFICULTY_PLAN.md` 任务 5）可以借鉴表达方式。

3. **最大的隐藏工作量：`ai-scoring.js` 要学会用意图信息。**
   `difficulty-check.mjs` 的玩家替身如果不会「看到预告就开盾」，
   量出来的就是一个无视核心机制的玩家，**所有难度数字失真**。
   这一条不做，任务 1 之后的每一个数字都不可信。

4. **冷却会连锁影响平衡。** `npm run balance` 和 8 关曲线全部要重跑。
   注意 `CLAUDE.md` 提醒的「采样均匀度」和「节奏」两行。

5. **`test/` 里有断言会被打破。** 已知：`combat.test.js` 锁 `calcStun` 概率的部分、
   `ai.test.js` 里与 `preferBasic` 相关的断言。改机制时一并重写，别注释掉。

6. **三处同步点**（`CLAUDE.md` 已警告过，本计划会再次踩到）：
   给技能加 `cd` / 改暴击条件时，`ai-scoring.js` 的评分、`combat.js` 的执行、
   `data.js` 的配置**必须一起改**。历史上「狂暴」加 `power` 时漏改一处，
   导致 AI 放狂暴不造成伤害、玩家放却会。

---

## 砍掉了什么（别再捡回来，除非触发条件成立）

- **~~把意图公开做成难度选项~~**（低难度显示、高难度不显示）。
  不做：这会让高难度退回「靠信息不对称制造难度」，正是本计划要修的东西。
  难度只能来自 AI 决策水平和属性两层。
- **~~保留眩晕的概率判定、只是降低概率~~**。
  不做：29.6 点的落差按比例缩小仍然远超玩家技术的 12.6 点，治标不治本。
- **~~给玩家也加命中率/暴击等正向随机来「找补」~~**。
  不做：输出随机加得越多，决策越不值钱，方向反了。
- **~~站位系统~~**：见任务 6，有明确触发条件。

---

## 任务 0 — 校准回归（**因为发现了一个真 bug 而新增**）

### 起因：`sim.js` 的行动经济和真实游戏对不上

做任务 1 抽 `nextActor` 时发现的。`sim.js` 的回合顺序是一个固定数组
`[p1a, p2a, p1b, p2b]` 逐个跑、跳过死人；而 `battle.js` 是**双方严格轮流
各出一个单位**。有人阵亡后两者不再等价：

| 场景 | 旧 sim.js (p1:p2) | battle.js (p1:p2) |
|---|---|---|
| 双方都是 2 人 | 40:40 ✓ | 20:20 ✓ |
| p2 死了一个 | 40:20 ✗ | 20:20 |
| **单人 BOSS vs 两人队** | **40:20 ✗** | **20:20** |

**旧模型里，一方一旦阵亡一个单位，行动次数立刻减半。** 而 `CLAUDE.md`
明文规定的规则是「队伍人数不影响行动次数，只影响血池」。
`campaign.js` 第 8 关的注释自己也写着「单人则每回合都出手——对最终战
来说这才像 BOSS」——**设计意图是对的，但用来校准的工具不是这么算的**。

### 后果：所有历史胜率数字都偏乐观

修好之后重跑 `campaign-check.mjs`：

| 关卡 | 目标 | 修复前 | **修复后** |
|---|---|---|---|
| 第 1 关 | 92% | 92.x% | 74.9% |
| 第 4 关 | 72% | 72.x% | 44.7% |
| 第 6 关 | 58% | 58.x% | 30.0% |
| **第 8 关 墨皇** | **42%** | **42.x%** | **1.9%** |

**墨皇偏了 40 个百分点，而且 1.9% 是完美玩家的数字。**
`depth-check` 上这一关的策略价值只有 **1.5 点**（完美 1.8% / 乱按 0.3%）——
用户说的「墨皇纯看运气」比实际情况还客气了：**它根本赢不了**。

- [x] `sim.js` 的回合循环改为和 `battle.js` 共用 `intent.js` 的 `nextActor`。
- [x] `MAX_ROUNDS` 60 → 120（回合含义变了，一个回合从 4 次行动变成 2 次）。
      副作用是好事：报告里的「平均回合数」现在和玩家看到的回合计数器同义。
- [ ] **全面重校 8 关曲线 + `DIFFICULTY_MODS`。**
      **排在任务 3 之后做**——任务 2/3 会把数值全部推翻一遍，
      现在校等于白校。但要知道：**在校准之前，战役后半段是打不过的。**

### 教训（值得写进 CLAUDE.md）

「回合顺序」这件事在 `sim.js` 和 `battle.js` 各有一份实现，
和历史上「战斗规则各写一份」是同一个病。**这次连规则文档都写对了，
错的只是其中一份实现**——所以「文档写清楚」防不住这类 bug，只有
「共用同一个函数」能防住。

---

## 基线快照（2026-08-26）

### ⚠ 修 `sim.js` 行动经济**之前**测的（已作废，仅供对照）

```
depth-check  策略价值：困难 45.5 / 墨皇 41.7 / 第8关 55.3
choice-check 平均主流技能占比 58.4%，无悬念回合 59.2%
```

### ✅ 有效基线（修复后，2000/1200 局）

改完拿这组数字对比：

```
depth-check  策略价值：困难 43.3 / 墨皇 32.1 / 第8关 **1.5**
             完美 vs 一般（困难档）：12.5 个百分点
choice-check 平均主流技能占比 58.0%，无悬念回合 **70.1%**
             死技能（<10%）：消失 0 / 狂暴 0 / 不屈 0 / 祝福 2 /
                             暗影弹 2 / 射击 5 / 净化 6 / 剑气 8
stun-check   眩晕落差 29.6 个百分点（修复前测的，改完眩晕要重测）
campaign     8 关全线低于目标，第 8 关 1.9%（目标 42%）
npm test     212/212
```

**「无悬念回合」从 59.2% 涨到 70.1%**：行动经济修正之后局面更常一边倒，
决策空间比原先估计的还要小。这让任务 2/3 更加必要，不是更不必要。

---

## 进度记录

- 2026-08-26　诊断完成，方案确认。新增 `depth-check.mjs` / `choice-check.mjs` /
  `stun-check.mjs` 三个诊断脚本并录下基线。用户确认范围：先只写文档；
  边界：8 个角色的身份和定位不变，技能可重配。**代码未动。**
