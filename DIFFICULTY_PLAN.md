# 难度重做 · 计划

> 持久化执行计划，供 context 重置后的新对话继续。
> 每完成一条把 `[ ]` 改成 `[x]`，并在末尾「进度记录」补一行。
> **每条任务都是自包含的**：只读本文件 + 对应代码就能动手。
> 前四份计划见 `REFACTOR_PLAN.md` / `AI_MERGE_PLAN.md` / `CAMPAIGN_PLAN.md` /
> `ENGINE_PLAN.md`（均已完成）。

---

## ▶ 当前进度：**任务 1 已完成，从任务 2 开始**

接手时先跑一遍确认基线：

```bash
npm test                        # 应为 187/187 通过
node difficulty-check.mjs 4000  # 注意：这把尺子本身就是本计划要修的东西
```

**动手前必读**：`CLAUDE.md` 的「工作约定」一节。

---

## 起因

2026-08-25，用户反馈：**「困难 AI 实在是太难了。玩家是打不出接近完美的做法的。」**
并提出：把现在这个最强 AI 做成隐藏模式，把困难调下来。

实测确认用户是对的，而且**根因不在 AI 强度，在校准用的尺子**。

---

## 根因：这个项目一直在拿「完美玩家」当尺子

`difficulty-check.mjs` 用 `aiHard` 当玩家替身。文件开头自己就写着：

> 用「同样是 hard AI、但没有属性加成」的一方来近似「打得不错的玩家」——
> 这是个乐观假设（真人不会每回合都算得这么准），所以这里跑出来的玩家胜率
> 应当视为**上限**：真人只会更低。

**它知道自己是上限，但难度还是照着这个上限校的。** 于是「困难 52.5%，
相对公平线 -7.3，略难但可接受」这个结论，描述的是一个不存在的玩家。

### 更糟的是：现有三档 AI 根本没覆盖真人所在的区间

拿三档 AI 分别当玩家替身，跑 3000 局/格：

| 玩家水平 | 自身公平线 | 打简单 | 打普通 | 打困难 |
|---|---|---|---|---|
| 困难水平 | 60.0% | 99.2% | 64.1% | **52.5%** |
| 普通水平 | 60.9% | 98.9% | 61.2% | **51.6%** |
| 简单水平 | 57.6% | 77.6% | 8.7% | **6.0%** |

看两件事：

1. **困难水平和普通水平当玩家，结果几乎一样**（52.5 vs 51.6）。
   说明 normal 和 hard 之间那点决策差距，作为玩家几乎不值钱。
2. **简单水平直接塌到 6%。** 普通和简单之间是 **45 个百分点的断崖**。
   真人就落在这个断崖里，而这里**一格分辨率都没有**。

而且 `easy` 不是「弱玩家」的好模型：它 `preferBasic: 0.7`，
**70% 的回合直接抡普攻**。真人会用技能，只是选得差、不集火、算不清账。
「只会平A」和「会用技能但选得差」是两种完全不同的弱。

---

## 新尺子量出来的真实数字

临时造了一条「失误率」坐标轴：**会用技能、按评分选，但有一定概率整回合凭感觉乱选，
且完全不集火、机会成本只算个大概**（`tempo 0.7 / teamwork 0`）。4000 局/格：

| 玩家失误率 | 打简单 | 打普通 | 打困难 |
|---|---|---|---|
| 0% | 99.0% | 61.5% | 51.5% |
| 10% | 98.5% | 54.1% | 45.1% |
| **20%** | 97.2% | 52.3% | **40.8%** |
| **35%** | 95.9% | 44.1% | **35.6%** |
| 50% | 92.3% | 35.4% | 26.4% |
| 70% | 87.1% | 24.3% | 19.6% |

**结论：一个正常玩家（失误率 20~35%）打困难只有 35.6%~40.8%。**
公平线是 60%（玩家恒定先手，先手值约 10 个百分点，这些数字已经含了这份优势）。
**相对公平线偏差 -19 ~ -24 个百分点。用户的体感是准的。**

顺带发现两件计划外的事：

- **普通档也偏难。** 对 20~35% 失误率的玩家只有 44~52%，同样低于公平线。
- **简单档对所有水平的玩家都是 87%~99%，完全没有区分度。**
  它不是「简单」，是「白给」——等于这一档不存在。

---

## 关键实测：难度到底装在哪个零件里

玩家固定为「失误率 25% 的人类模型」，逐个旋钮单独关掉，5000 局/格：

| 改动 | 玩家胜率 | 变化 |
|---|---|---|
| 困难现状 | 38.2% | ← 基准 |
| **只去掉属性加成（攻 +7% / 回蓝 +10%）** | **46.6%** | **+8.4** |
| 只关掉集火（teamwork 1→0） | 39.5% | +1.3 |
| 只关掉前瞻（tactical off） | 37.6% | -0.6 |
| 只把 noise 2→20 | 41.3% | +3.1 |
| 只把 tempo 1→0.5 | 38.0% | -0.2 |
| 去加成 + 关集火 | 47.8% | +9.6 |
| 去加成 + 关集火 + noise 20 | 49.0% | +10.8 |

**难度几乎全部装在属性加成里（+8.4），所有行为旋钮加起来才 +2.4。**

这不是新发现，是**第三次**撞到同一件事。`CLAUDE.md` 里早就记着：

> AI 决策水平本身拉开的差距比想象中小——之前一直是那个 +15% 攻击在撑"困难"的体感。

**把它当成这个项目的一条稳定事实：想调难度就调属性，调 AI 行为基本没用。**

---

## 别人怎么做的

查了一圈，几条和本项目直接相关的：

- **AI 的任务是「看起来聪明」，不是「下得最好」。** 回合制游戏里人类的直觉和
  算计本来就能碾压 AI，设计目标是让 AI 的攻防看起来经过思考、构成挑战，
  但最终让玩家赢下来。
- **「打败一个笨蛋，远不如打败一个高手爽。」** 这条很关键，
  **它直接反对「把 AI 变笨来降难度」这个最直观的做法**。
- **不可预测性是刚需。** 玩家摸清 AI 套路之后游戏会迅速变无聊，
  所以行为需要有变化。
- **handicap（属性差异）是被广泛接受的手段**，和 cheating（让 AI 看到玩家
  看不到的信息）要分开看。低难度反向 handicap（给玩家多点血/伤害）同样是常规做法。

来源：
- [Designing AI Algorithms For Turn-Based Strategy Games](https://www.gamedeveloper.com/design/designing-ai-algorithms-for-turn-based-strategy-games)
- [Toward Smarter Opponents: Rethinking AI in Turn-Based Games](https://amt-lab.org/blog/2026/3/rs-turn-based-game-ai)
- [AI Cheating In Games](https://n-ahamed36.medium.com/ai-cheating-in-games-583e333677ce)
- [The Computer Is a Cheating Bastard](https://tropedia.fandom.com/wiki/The_Computer_Is_a_Cheating_Bastard)

**这些和上面的实测正好互相印证**：行为旋钮既不值钱、调了还会让 AI 变蠢变无聊；
属性 handicap 既是唯一有效的杠杆、又是业界标准做法。**所以方向是调属性，不是调脑子。**

---

## 设计结论（本计划的三条原则）

1. **不 lobotomize。** 完整的聪明 AI 保留下来，整体挪进隐藏档。
   困难档用同一套决策，只在属性层放松。
2. **难度旋钮 = 属性，不是行为。** 实测支持，也不用动评分模型那块地基。
3. **要让玩家看得见 AI 在犹豫，而不是偷偷变弱。**
   偷偷变弱既没效果（+1.3）又违背「打败高手才爽」。

---

## 目标曲线

统一以「失误率 25% 的人类模型」为基准玩家，公平线约 60%：

| 档位 | 现状 | 目标 | 理由 |
|---|---|---|---|
| 简单 | ~96% | **85%** | 现在对谁都是白给，没有区分度 |
| 普通 | ~48% | **65%** | 略高于公平线，「默认档应该能赢」 |
| 困难 | **38%** | **50%** | 明显偏难但赢得下来 |
| 隐藏（噩梦） | — | **~35%** | 就是现在的困难，原样搬过去 |

---

## 任务 1 — 造一把「人类水平」的尺子

**这是所有后续任务的前提。** 尺子不换，后面每一步都是在错的坐标系里调。

- [x] ~~新增 `ai-human.js`~~ **改为：`ai.js` 的 `decide` 改吃 cfg 对象，导出 `makeAi(cfg)`。**
      动手前先测了一件事：**现有的 `noise` 旋钮能不能直接当人类坐标轴用？**
      能——实测 noise 30/60/100 拉出的梯度，和「每回合 10%/25%/40% 概率乱选技能」
      几乎重合（打困难 46.5/41.5/34.4 vs 46.3/39.1/31.8）。
      **所以原方案的 `mistake` 字段和整个新文件都是多余的**：
      玩家替身用现有 cfg 形状就能造，零新增决策代码。
- [x] `difficulty-check.mjs` 改用 `makeAi` 造替身，**同时报三档玩家**
      （熟手 noise 30 / 一般 noise 60 / 生手 noise 100），以「一般玩家」为校准基准。
- [x] 公平线改成「该水平的玩家自己打自己、双方无加成」，不再用 hard 对镜那条线。
- [x] 顶部注释重写。

**没有新开文件，但那条顾虑仍然成立**：`ai.js` 的三档是**游戏内容**，
玩家替身是**测量工具**。所以 `makeAi` 的注释里明写了「不要把新 cfg 注册成
第四个难度档」——玩家永远不该在 UI 里见到它。

**验证**：用新尺子重跑，应当复现上面那张失误率表（±2 个百分点内）。
**工作量**：1 小时。

---

## 任务 2 — 先把战役和人机难度解耦

**必须在任务 3 之前做，否则改 `hard` 会把战役曲线整个打塌。**

`campaign.js` 里第 6 / 7 / 8 关写的是 `difficulty: 'hard'`，
直接引用人机模式的困难档。战役曲线（93.7 → … → 41.6）是照现在这个 hard
一格一格校出来的，**hard 的定义一变，这三关立刻跟着变，而且是白白变**——
战役有自己的 `enemyMod` 属性旋钮，不需要蹭人机的难度档。

- [ ] 战役的 `difficulty` 字段改成只表示**决策档位**，与人机难度档解耦。
      两种做法二选一，建议前者：
      1. 战役固定引用「完整版 AI」（也就是任务 3 之后的隐藏档决策水平），
         难度全靠各关自己的 `enemyMod` 调——**战役本来就是这么设计的**，
         `CAMPAIGN_PLAN.md` 明确写过「战役不走 `applyDifficulty`」。
      2. 给战役单独一份决策档位表。
- [ ] 改完立刻 `node campaign-check.mjs 5000`，曲线必须和现在一致
      （93.7 → 86.0 → 78.8 → 74.2 → 65.8 → 59.0 → 50.1 → 41.6，容差 ±2.5）。

**验证**：`campaign-check` 曲线不动 + `npm test` 全绿。
**工作量**：40 分钟。

---

## 任务 3 — 把现在的困难整体挪成隐藏档

- [ ] `ai.js` 的 `DIFFICULTY` 加一档 `nightmare`，**原样复制现在 `hard` 的四个值**
      （`noise 2 / tempo 1 / teamwork 1 / tactical true`）。
- [ ] `combat.js` 的 `DIFFICULTY_MODS` 加 `nightmare`，
      **原样复制现在 `hard` 的属性加成**（攻 ×1.07 / 回蓝 ×1.1）。
- [ ] 名字建议贴世界观：**「墨皇」**（战役最终 BOSS，玩家通关后会认得这个名字），
      或直白的「噩梦」。
- [ ] **解锁条件**：建议「通关战役」。
      **不要另存 localStorage key**——直接从已有的 `inkfight_campaign`（已通关数）推。
      `CAMPAIGN_PLAN.md` 里记过：同一份知识两份实现迟早对不上，
      **这个项目已经因此出过三次 bug**。
- [ ] UI：`initDifficultyScreen()`（`main.js`）加第四个按钮，未解锁时**不显示**
      （不是灰掉——隐藏模式被剧透就不叫隐藏了）。解锁后可以加个小提示。

**验证**：未通关时看不到第四档；通关后出现；选中后实测 AI 行为与现在的困难一致。
**工作量**：1.5 小时。

---

## 任务 4 — 重新校准简单 / 普通 / 困难

**只调属性，不动行为旋钮**（实测理由见上）。主战场是 `combat.js` 的 `DIFFICULTY_MODS`。

- [ ] **困难**：先把属性加成整个去掉（实测 38.2% → 46.6%），量一遍。
      离 50% 目标还差一点的话，考虑给玩家一侧一点点反向 handicap，
      或者给 AI 一个小减益（例如回蓝 ×0.95）。
      **决策水平保持不变**——它就该是「完整的聪明 AI，只是没有属性优势」。
- [ ] **普通**：现在约 48%，目标 65%。需要给 AI 上减益（属性向下）。
      注意 `DIFFICULTY_MODS.normal` 目前是 `null`，要新建一条。
- [ ] **简单**：现在约 96%，目标 85%——**这一档是要变难的**。
      现在 `easy` 的 `preferBasic: 0.7` 让它 70% 回合抡普攻，
      既白给又不像人。建议降到 0.2~0.3，让它会用技能只是用得差，
      再靠属性减益兜住 85%。
- [ ] 每档跑 `node difficulty-check.mjs 5000`，三档玩家（失误率 0.1 / 0.25 / 0.4）
      都要看，**单调性比绝对值更重要**：任何一档对任何水平的玩家，
      都不该出现「简单比普通还难」这种翻转。

**验证**：目标曲线 ±5 个百分点内；`npm run balance` 角色胜率不动
（改的是难度档，不该影响角色平衡——balance 跑的是无加成对镜）。
**工作量**：2~3 小时（大部分时间在跑模拟）。

---

## 任务 5 —（可选）让 AI 的失误看得见

前四条做完难度就正常了。这一条是**体验**，不是数值。

研究里那两条——「要看起来聪明」和「需要不可预测性」——现在这个 AI 一条都不满足：
它 `noise: 2`，几乎恒定选最优解，从玩家视角看就是一台不会犹豫的机器。

- [ ] 低难度的失误改成**看得见**：AI 选了次优技能时，在战斗日志里给一点表现
      （比如「敌方似乎犹豫了一下」），而不是静默变弱。
- [ ] 或者反过来做**性格化**：给不同角色不同的行为倾向（狂战士更莽、守卫更缩），
      让 AI 的选择有可读的动机。这比单纯加噪声更接近「看起来聪明」。

**这条没有实测支撑，纯设计判断，做不做都不影响难度曲线。**
**工作量**：不定，建议单独一轮。

---

## 砍掉了什么（别再捡回来，除非触发条件成立）

- **动态难度调节（DDA / 橡皮筋）— 砍。**
  这个游戏平均 **8.6 回合**就分胜负，等检测到「玩家在挣扎」这局已经结束了。
  而且 DDA 一旦被玩家察觉，体验比固定难度还差（「我赢是因为系统放水」）。
  → **重新考虑的条件**：战斗长度显著变长（比如 20 回合以上）。

- **给 AI 加信息限制（不让它看到玩家 SP / 精确血量）— 砍。**
  听起来最「公平」，但实测唯一用到敌方 SP 的是 `tacticalBonus` 的眩晕判断，
  单独关掉值 **-0.6 个百分点（噪声）**。改了不解决任何问题。
  → **重新考虑的条件**：出现真正吃信息差的机制（比如隐藏手牌）。

- **重写评分模型让 AI「更像人」— 砍。**
  `ai-scoring.js` 是整个项目的地基：`npm run balance`、`campaign-check`、
  战役全部 8 关的曲线都建在它上面。为了难度去动它，等于把四份已完成计划的
  校准结果全部作废。**人类模型应该是另一个文件（任务 1），不是改地基。**
  → **重新考虑的条件**：评分本身被证明有 bug（不是「不够像人」）。

- **给困难 AI 降智（关集火 / 关前瞻 / 加大噪声）— 砍。**
  实测三个加起来才 +2.4 个百分点，而且**研究明确说这个方向是错的**：
  「打败一个笨蛋远不如打败一个高手爽」。
  → 唯一的例外是 `noise`：它值 +3.1，且「偶尔选次优」本身就是任务 5 想要的
    不可预测性。要用也是当调味品，不是主料。

---

## 回归网（每条任务改完都拿它对）

```bash
npm test                        # 187/187
npm run balance 10000           # 角色平衡：牧师63.5 弓手55.0 术士54.5 守卫54.3
                                # 剑士46.2 法师45.5 刺客42.9 狂战士38.5（±2.5 噪声）
node campaign-check.mjs 5000    # 93.7 → 86.0 → 78.8 → 74.2 → 65.8 → 59.0 → 50.1 → 41.6
node difficulty-check.mjs 5000  # 任务 1 之后是新尺子，旧数字不可比
```

**`npm run balance` 不该被本计划影响**（它跑的是无加成对镜）。动了就说明
改到了不该改的地方——多半是碰了 `ai-scoring.js`。

**`campaign-check` 在任务 2 之前会被 hard 的改动带偏**，这正是任务 2 要先做的原因。

---

## 风险与应对

- **风险：改 `hard` 把战役曲线打塌。** 这是最大的一个，8 关里有 3 关引用它。
  → 应对：任务 2 先解耦，之后每条任务都跑 `campaign-check`。
- **风险：人类模型造得不像，新尺子照样是错的。**
  → 应对：`mistake` 不要只取一个值，永远报 0.1 / 0.25 / 0.4 三档；
    结论只采信「三档都同向」的改动。**能被真人验证最好**——
    用户自己打几局，看实际胜率落在哪一档附近。
- **风险：属性减益调过头，AI 变成沙包。**
  → 应对：目标曲线留了 ±5 的容差，且要看**单调性**而不是单点数值。
- **风险：隐藏档解锁条件另存了一份进度。**
  → 应对：从 `inkfight_campaign` 推，不新增 key。这个项目已经因为
    「同一份知识两份实现」出过三次 bug。

---

## 建议顺序与中断策略

**1 → 2 → 3 → 4 →（可选 5）**。

任务 1 是前提（尺子），任务 2 是安全网（解耦），任务 3 是纯搬运（低风险），
任务 4 才是真正调数值的地方。**把调数值放最后，是因为前三条做完之后
才有一个可信的坐标系可以调。**

- 每条任务结束时代码都应处于**可提交、测试全绿**的状态，**一条一个 commit**。
- 直接提交 `main`，不开分支（见 `CLAUDE.md` 工作约定）。
- 若中途 context 重置，从本文件第一个未勾选项继续即可。

---

## 进度记录

- 2026-08-25：写下本计划。起因是用户反馈「困难太难，玩家打不出接近完美的做法」。
  实测确认并定位到根因：**校准用的尺子是「完美玩家」**，
  而现有三档 AI 在玩家替身这个用途上，`easy` 和 `normal` 之间有 45 个百分点的
  空白，真人正好落在里面。新造的「失误率」坐标轴量出真实数字：
  正常玩家打困难 **35.6%~40.8%**，相对公平线 -19 ~ -24。
  另外量到两件计划外的事：**普通档也偏难**（44~52%），
  **简单档对谁都是 87%+、完全没有区分度**。
  旋钮拆解显示难度几乎全装在属性加成里（+8.4），行为旋钮合计才 +2.4——
  这已经是这个项目第三次撞到「AI 决策水平拉不开难度」这件事。
  查到的业界做法（AI 要「看起来聪明」而不是「下得最好」、
  「打败笨蛋不如打败高手爽」、handicap 是标准手段）与实测互相印证，
  所以方案定为**保留完整 AI 挪进隐藏档 + 只在属性层调难度**，
  而不是把困难 AI 降智。
