# 墨境之战 (Ink Realm Clash)

火柴人回合制对战游戏。纯 HTML5 + Canvas + ES modules，**零运行时依赖，无构建步骤**。
双人 / 人机 / 8 关剧情战役 / 平衡测试四种模式，8 名角色、3 张战场。

## 运行

日常入口是桌面快捷方式**「墨境之战.lnk」**（指向 `launch-game.vbs`：隐藏窗口拉起
静态服务器，再自动打开浏览器）。

命令行等价物：

```bash
node serve-game.mjs
```

然后访问 <http://localhost:5566/inkfight.html>。这个服务器零依赖，端口被占用时会
直接退出（假定已经在跑），所以重复启动是安全的。

也可以用 `npx serve .` 或 VS Code Live Server。
**必须走 HTTP** —— ES modules 不能从 `file://` 加载。

## 四种模式

| 模式 | 说明 |
|------|------|
| 双人对战 PVP | 同一台电脑两人轮流操作，各控 2 名角色。开局有 **BAN 阶段**（只有 PVP 有） |
| 人机对战 | 玩家 1 手操，玩家 2 交给 AI，三档难度 |
| 战役模式 | 8 关剧情战役。固定主角剑士「墨白」，每通一关解锁一名队友，最终关单挑墨皇 |
| 平衡测试 | 双方全 AI 自动跑多局并统计胜率（命令行版见下面的 `npm run balance`） |

**难度是两层：AI 决策水平 + 属性加成。**

| 难度 | AI 行为 | 属性加成 |
|------|---------|----------|
| 简单 | 决策噪声大，偏爱普攻，几乎不针对残血 | 攻击力 ×0.85，初始 SP 只有一半 |
| 普通 | 按评分选技能和目标，会集火但配合意识只有一半 | 标准属性 |
| 困难 | 整队集火同一目标、不重复上增益、队友濒危时顶上去、不浪费回合 | 攻击力 ×1.07，SP 回复 ×1.1 |

战役模式另有一条独立的属性旋钮（每关的 `enemyMod`），不走上面这张表。

进度和设置存在 `localStorage`：`inkfight_campaign`（已通关数）、
`inkfight_campaign_totals`（每关战绩）、`inkfight_muted`（静音）、`inkfight_sim`（最近 10 次平衡测试的结果）。

## 玩法

- 双方各 2 名角色，**交替各行动一个单位**——队伍人数不影响行动次数，只影响血池总量。
- **HP** 生命值，**SP** 灵能值（放技能的消耗，每回合自动回复一部分）。
- **SP 越满越容易被眩晕**：眩晕类技能（法师「灵能过载」、弓手「束缚箭」）的命中概率
  = 基础概率 + 系数 × (目标当前 SP ÷ SP 上限)。留着满 SP 不放是有代价的。
- **暴击** ×1.5 伤害；**护盾**优先承伤；**嘲讽**强制敌方集火。
- 每个角色有一个**被动**，在回合开始 / 暴击 / 受击等节点自动触发。
- 键盘 `1`~`4` 释放技能，`ESC` 取消选目标 / 退出战斗。
- 战场效果会影响全场的伤害或 SP 回复。

## 角色

| 角色 | 定位 | HP / SP / ATK | 被动 |
|------|------|---------------|------|
| 剑士 | 均衡输出 | 125 / 80 / 18 | 剑意：暴击后回 8 SP |
| 法师 | 高爆发·控制 | 92 / 130 / 15 | 法力涌动：回合开始 SP≥80% 时，下次技能伤害 +20% |
| 守卫 | 坦克·反制 | 155 / 65 / 14 | 铁甲反弹：受到伤害时反弹 10% 给攻击者 |
| 刺客 | 高伤·脆皮 | 95 / 100 / 18 | 暴击蓄能：暴击后回 8 SP |
| 牧师 | 治疗·辅助 | 108 / 115 / 10 | 圣光庇护：回合开始时若友方 HP<30%，自动治疗 12 |
| 狂战士 | 越残越强 | 140 / 70 / 19 | 血怒：HP<40% 时受击攻击 +10%（叠 3 层） |
| 弓手 | 远程·灵活 | 105 / 90 / 19 | 鹰眼：每回合开始暴击率 +3%（叠 4 层） |
| 术士 | 腐化·爆发 | 100 / 120 / 14 | 腐化侵蚀：打有腐化层的目标额外造成 层数×8 伤害 |

## 战场

| 场景 | 效果 |
|------|------|
| 墨色虚空 | 无加成，考验真实战力 |
| 赤焰熔岩 | 所有伤害技能 +15% |
| 碧蓝灵泉 | 每回合开始所有单位额外 +5 SP |

## 模块结构

游戏逻辑全部在根目录的 ES modules 里，`inkfight.html` 只剩结构和样式。

| 文件 | 职责 |
|------|------|
| `inkfight.html` | HTML 结构 + CSS |
| `main.js` | 入口：UI 流程、事件监听、`window` 暴露 |
| `data.js` | 场景与角色数据（`SCENES` / `CHARACTERS`） |
| `state.js` | `gameState` 与工具函数 |
| **`combat.js`** | **战斗规则引擎（纯函数）。`battle.js` 和 `sim.js` 唯一的规则真相来源** |
| `battle.js` | 回合流程编排 + DOM 渲染 + 音效特效（规则计算全部委托 `combat.js`） |
| **`ai-scoring.js`** | **技能评分的唯一实现**：把各类技能收益折算成「等效伤害」以便横向比较 |
| `ai.js` | 难度包装：`aiEasy` / `aiNormal` / `aiHard`。纯函数，可在 Node 里跑 |
| `sim.js` | 无头战斗模拟器（平衡测试用），规则来自 `combat.js`，决策直接调 `aiHard` |
| `campaign.js` | 战役数据：8 关阵容 / 剧情 / 场景 / AI 档 / `enemyMod`，主角与队友解锁表 |
| `render.js` | 战斗画面渲染与单位动画 |
| `stickman.js` | 火柴人与武器的 Canvas 绘图（纯绘图，无副作用） |
| `vfx.js` | 技能特效、飘字、粒子 |
| `scene.js` | 场景背景、菜单粒子、场景预览 |
| `audio.js` | BGM 与音效（Web Audio 合成，无音频文件） |
| `serve-game.mjs` | 零依赖静态服务器 |
| `launch-game.vbs` | 桌面快捷方式的启动脚本（**必须保持纯 ASCII**） |
| `test/` | 单元测试，`node --test` 跑 |

## 开发者命令

```bash
npm test                          # 单元测试（Node 内置 node --test，无外部依赖）
npm run balance                   # 无头模拟 4000 局，输出各角色胜率 + 采样均匀度
npm run balance 10000             # 指定局数
node difficulty-check.mjs 6000    # 难度公平性：玩家打各档 AI 的胜率
node campaign-check.mjs 3000      # 战役难度曲线：玩家打 8 关各自的胜率
```

判读这几个数字有坑（先手值约 10 个百分点、`npm run balance` 只能发现离群角色等等），
动手改数值前先看 `CLAUDE.md` 的「工作约定」一节。

## 项目文档

- `CLAUDE.md` —— 工作约定、架构说明、历次改动的原因和实测数据。**改代码前先读。**
- `REFACTOR_PLAN.md` / `AI_MERGE_PLAN.md` / `CAMPAIGN_PLAN.md` —— 已完成的三份计划。
- `ENGINE_PLAN.md` —— 进行中的维护性改进计划。
