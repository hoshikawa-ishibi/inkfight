# 改动历史 · inkfight

## 2026-09-05 · 界面收束

战斗移除重复导航和常驻视角工具条，将锋芒、共享墨量、收笔与技能合为一个底部操作区；战斗记录保持直接入口，视角、规则、设置和离场按需展开。保留当前状态与伤害预估，减少重复提示。

对战与远征共用深色页签编队器：预设、自由编队、已保存分别显示。远征将大地图收为三关进度条，缩短标题与卡片，手机行囊默认折叠。首页适配低屏幕高度；图鉴、战绩与结算保留固定操作，将长内容限制在局部列表。玩法、数值、立绘与存档格式保持不变。

1280×720 与 390×844 浏览器检查主要操作完整可见；472 项回归通过。详见 [布局验收](ui-fixes-validation.md#最后一轮界面收束)。

## 2026-09-05 · 根据实际游玩反馈修复页面与战斗信息

修复远征样式覆盖页面隐藏规则，导致进入战斗后旧远征仍占位、必须下拉才能看到战场的问题。页面统一 hidden/inert/active；验收台新增真实渲染可见性和滚动检测。首页重建为同一居中容器内的正文与立绘，覆盖 2817×1486 宽屏。

战报改为固定在视口内的“战斗记录 / 角色详情”面板。队员卡与技能区常驻锋芒，说明 100 点触发 1.5 倍重击，并标出可触发重击的技能或多段中的具体段数。

远征原状态机并未提前结束，但每关胜利都使用“此路已破”造成误导；改为阶段专属标题、已完成 x/3、奖励/营地/下一关动作，第三胜才显示完整通关。首页继续入口也按存档阶段显示下一步。立绘与战斗数值未改。

## 2026-09-05 · 三墨同心

所有新战斗统一为四人全队共享墨量；删除旧个人 SP、战役入口、属性加成难度和多页备战。加入 10 套预设与可保存的自由编队。桌面重排为一个视口内的放大 3D 主战场、紧凑队员条、墨量与技能面板；手机使用双排队员和双列技能。

额外完成实际结果预览、收笔护盾提示、单招观战、同阵/换边重赛、关键落笔总结、开放图鉴、减少动态与独立音量、规则版本筛选。实战、预览与模拟共用技能执行器。原有 AI 立绘未修改，旧记录和分享码继续可读。

七组固定策略对照各 2,000 局自由对战与 200 次远征，落地瘟疫 2 墨、医仙治疗 15/34/14；保留仍适用的战斗回归覆盖。方法、浏览器流程、结果与限制见 [本轮验收](shared-ink-validation.md)。旧计划移入归档，后续候选集中在 [当前方向](DIRECTION.md)。

## 早期记录

> 当前规则以 [`docs/DIRECTION.md`](DIRECTION.md) 与 `CLAUDE.md` 为准。本文件所有 SP、单人出手、轮空回蓝、封印额外行动和战役平衡记录均为历史背景，不能作为共享墨新开发的约束。

> 从 `CLAUDE.md` 迁出来的详细改动记录（2026-08-26）。
> 迁出的原因：这些内容是**历史参考**，不是每次会话都要遵守的约束，
> 而 `CLAUDE.md` 每开一个新会话都要整份读进上下文。
> 里面那些「别再犯」的硬规则已经提炼进 `CLAUDE.md` 的「踩过的坑」一节，
> **这里保留的是完整经过、实测数字和当时的推理过程**——
> 想知道某个数值为什么是现在这个值，来这里翻。

条目按原顺序保留，未做改写。

- **抽出 `combat.js` + 补测试**（2026-08-24）：`battle.js` 658→557 行；`sim.js` 删除重复实现。
  修复两个既有 bug：(1) `sim.js` 漏抄 `corruptBonus` 被动和 `plague`/`corruptBurst` 技能类型，
  术士整套机制在平衡测试里静默失效；(2) `randomPicks` 用 `sort(()=>Math.random()-0.5)` 洗牌，
  角色入选率偏到 41%~58%（应为 50%），改为 Fisher-Yates。详见 `REFACTOR_PLAN.md`。
- **重写 `sim.js` 的 `pickSkill` + 平衡调整**（2026-08-24）。
  起因：旧 `pickSkill` 只给 `damage`/`heal`/`stun`/`drain` 四种技能打分，其余 13 种只拿
  `cost×0.5`。实测几千局里**术士的瘟疫一次都没被使用过**，守卫的铁壁/嘲讽、
  牧师的净化/祝福同样被系统性忽略——**这意味着此前所有胜率数据都不反映角色真实强度**。
  - 新评分把各类技能的收益统一折算成「等效伤害」，并让条件性技能（净化 / 腐化爆发 /
    治疗）在条件不满足时得 0 分，避免浪费回合。`pickTarget` 同步修正：净化只找真正
    带 debuff 的队友，治疗按缺失血量选，加攻给输出最高的队友。
  - 修复过程中发现并修掉两个评分 bug：`healSp` 的自损惩罚不随血量放大（剑士 30 HP
    残血时仍用「剑气」自损 18 HP，属自杀式回蓝）；`selfBuff` 漏乘技能倍率导致算出负分。
  - **修复前后对比（同为 4000 局）**：牧师 39.6% → 58.2%（从全场最弱变第三强），
    守卫 46.7% → 60.0%，术士 70.1% → 58.4%。**修复前的数据是错的，不要参考。**
  - 数值改动：剑士 `spRegen` 8→10、破甲突刺 `power` 1.8→2.1。
    依据：剑士是纯单体输出且无工具技能，却背着全场最低回蓝，大招占 SP 池 44%
    （法师同为 35SP 但只占 27%），且同成本下法师大招伤害高出 43%。
  - **当前平衡（10000 局，采样极差 2.9%）**：守卫 56.7% / 术士 54.9% / 牧师 53.9% /
    法师 50.6% / 狂战士 48.3% / 剑士 45.8% / 弓手 45.6% / 刺客 44.2%。
    跨度从最初的 27.6 个百分点收窄到 12.5，无离群角色。
- **修掉零使用技能 + 补上机会成本建模**（2026-08-24）。
  - **根本原因是评分漏了一项**：辅助技能要占掉一整个回合，但评分从没把
    「这回合本可以打出的伤害」算作代价，导致 buff / 护盾 / 嘲讽类技能分数虚高。
    加上 `tempo` 机会成本后，平衡跨度从 21 收窄到 7.3 个百分点——**这是本轮
    改动里效果最大的一处，比任何数值调整都管用。**
  - **设计原则：纯功能性技能在这个战斗节奏下需要附带即时收益。**
    「花一整回合只给 buff」几乎永远不划算（狂暴实测：增伤刚好被少打的那回合抵消，
    还倒亏血）。因此给眩晕类和自我增益类技能加了 `power` 字段，改成边打边生效：
    - `combat.js` 新增 `resolveStun` / `resolveSelfBuff`，带 `power` 时先结算伤害再上效果
    - 法师「灵能过载」140% 伤害 + 眩晕、弓手「束缚箭」130% + 眩晕、
      狂战士「狂暴」80% 伤害 + 3 回合攻 +35%
  - **buff 强度不再硬编码**。此前 `0.4`/`0.3`/`0.25`/自损 `8` 散落在
    `combat.js`/`sim.js`/`battle.js` **三处**，改个数值要动三个文件的代码。
    现已集中为 `BUFF_DEFAULTS` + `makeSelfBuff`/`makeAllyBuff`/`makeSpBuff`
    工厂函数，data.js 可用 `buffValue` / `selfDmg` / `debuffValue` 覆盖。
    **调数值不必再改代码。**
  - 数值改动：牧师祝福 35SP/+30%/2回合 → 28SP/+50%/3回合。
  - **当前平衡（10000 局，采样极差 3.4%）**：牧师 55.3% / 守卫 53.1% / 术士 51.2% /
    法师 50.3% / 弓手 49.9% / 剑士 49.8% / 狂战士 46.8% / 刺客 43.7%。
    跨度 11.6，无离群角色。
  - 仍属低使用率但**合理**的技能：免费普攻（有更好技能时自然不用）、
    刺客「消失」/ 狂战士「不屈」等保命技能（本就是情境技能）。
- **合并两套 AI + 让 AI 学会配合**（2026-08-24，`AI_MERGE_PLAN.md` Phase 1～4）。
  技能评分收敛为 `ai-scoring.js` 一份，`ai.js` 只做难度包装，`sim.js` 直接调
  `aiHard`。**平衡测试第一次真正测的是玩家面对的那个 AI。**
  - **AI 配合**：集火（按「威胁 / 有效血量」选目标，选定后整队不换人）、
    不重复上 buff、队友濒危时坦克顶上去、按产出选治疗目标、
    不用大招收残血（伤害按目标有效血量封顶）。
    强度由 `teamwork` 权重分档：easy 0 / normal 0.5 / hard 1。
  - **过程中挖出两个 AI 盲区**（都是评分的错，不是角色强弱）：
    (1) 威胁度只算攻击力，牧师是全场最低，**敌方奶妈从来没被列入过集火名单**，
    胜率虚高到 65.2%；改为 `max(伤害产出, 治疗量)`。
    (2) 「优先救输出高的队友」的权重误乘进了分数，等于让治疗整体涨价 50%。
    **教训：排序用的权重和绝对分数必须分开**——混在一起会让「在 A、B 之间选 A」
    悄悄变成「A 这件事整体更值得做」。
  - **当前基线（10000 局，AI=hard，采样极差 3.7%，平均 8.7 回合分胜负、
    0.5% 超时）**：牧师 63.6% / 弓手 55.3% / 术士 54.0% / 守卫 53.3% /
    剑士 46.3% / 法师 45.2% / 刺客 43.2% / 狂战士 39.1%。
    跨度 24.5。**这组数字和 Phase 4 之前的不可比**（换了 AI），也不是回归——
    以前的数字测的是另一个 AI。
  - **AI 变强会放大角色差距**：集火让脆皮更吃亏（法师、刺客下滑），
    不浪费回合让辅助更受益（牧师上升）。狂战士 39.1% 最低，
    因为它的 `getEffectiveAtk` 随掉血上升、有效血量随掉血下降，
    在「威胁 / 有效血量」的集火模型里会被越打越优先，形成死亡螺旋。
  - 已确认**不是**评分盲区：净化 / 腐化爆发在条件满足时使用率 100%；
    「狂暴」使用率 0% 是技能本身算不过账（3 回合多打约 7 点伤害，
    却要自损 18 HP 并放弃「鲜血之力」的吸血），属角色数值问题。
  - **浏览器实测（困难）实录到完整配合链条**：`牧师 祝福 → 刺客`（加给输出最高的
    队友而非自己）、`刺客 暗影突袭 → 法师` + `牧师 光击 → 法师`（两个单位集火同一
    目标）、法师阵亡后两人同时改打守卫（集火目标随死亡切换）、收人头用免费的
    「匕首」而不是 30SP 的「暗影突袭」。简单难度则连续平A，守卫全程不开铁壁嘲讽。
  - **实测抓到一个单测没覆盖的回归**：`pickTarget` 在全队满血时给治疗返回 `null`
    （净化同理），简单难度的噪声照样会选中它，于是 `battle.js` 在 `target.hp`
    上抛异常、整局卡死。已修并补了 4 条测试。详见上面的「评分为 0 是软约束」。
  - **角色数值调整留到下一轮**，不在合并 AI 这条线里做。
    当前跨度 24.5：牧师 63.4% 偏强、狂战士 38.9% 偏弱（后者有明确机制原因，
    见 `AI_MERGE_PLAN.md` 文末）。
- **困难难度属性加成减半**（2026-08-24）。用户反馈"完全打不过困难"，实测确认：
  玩家胜率只有 42.4%，而**先手对镜的公平线是 59.8%**（先手值约 10 个百分点，
  以前一直拿 50% 当基准，等于把难度判断建立在错的尺子上）。
  - **根因是双重加成**：攻 +15% 是给合并 AI 之前那个笨 AI 配的，
    现在 AI 本身会集火、不浪费回合，两层难度叠在一起就过头了。
  - **拆开量才发现攻击是罪魁**：只留攻 +15% 是 -13.8，只留回蓝 +20% 只有 -2.3。
  - 改为攻 +7% / 回蓝 +10%，落在 52.5%（相对公平线 -7.3）。
    梯度：简单 99.1% / 普通 64.2% / 困难 52.5%。
  - 当时也考虑过全去掉（60.4%），但那样普通和困难只差 3 个百分点，
    难度选项名存实亡——**AI 决策水平本身拉开的差距比想象中小**，
    之前一直是那个 +15% 攻击在撑"困难"的体感。
  - 数值收敛到 `combat.js` 的 `DIFFICULTY_MODS`，`battle.js` 与
    `difficulty-check.mjs` 共用，避免"调了一处、量的却是另一套数"。
- **修掉 `gameState.turnOrder` 的三处死读**（2026-08-24）。回合流程早先从
  「turnOrder 数组 + 下标」改成了 `currentPlayer` + `p1/p2LastActed`，但三个读取方
  没跟着改，读到的永远是 `undefined`——**而且全都静默失败，不报错**：
  - `cancelTargeting()` 拿不到当前单位 → 按 ESC 取消选目标后，`waitingForTarget`
    已置 false（点角色没反应）而技能面板没重绘（还停在「请选择敌方目标」），
    **这一回合彻底无法行动**。
  - 数字键 1~4 快捷键**完全失效**，尽管技能按钮上就写着 `[1] [2] [3] [4]`。
  - 当前行动单位的 `active-turn` 高亮**永远不亮**。
  改法：新增 `gameState.activeUnitId`，由 `activateUnit()` 单点写入，三处统一读它；
  `cancelTargeting` 优先用手上就有的 `pendingActor`（更精确）。
  删除 `turnOrder`/`currentIdx`。
  **教训：重构掉一个状态字段时，要 grep 干净所有读取方。** 这三处的共同点是
  「读到 undefined 之后走了一条看似正常的分支」（`if(u)` 直接跳过、
  `===` 恒为 false），不抛异常，所以能潜伏很久。
- **战役角色与剧情绑定 + 曲线重新校准**（2026-08-25，`CAMPAIGN_PLAN.md` Phase 4）。
  - 战役有固定主角了：`CAMPAIGN_HERO` = 剑士「墨白」，每关预选不可换；
    队友池 7 人各有剧情名，每通一关解锁一个（顺序按「弱的先给」排，牧师最后）。
    选角界面四态：主角锁定 / 可选 / **本关敌方**（禁选）/ 未解锁。
  - **解锁进度不另存 key**，直接从 `inkfight_campaign`（已通关数）推——
  （2026-08-25 追加：这个决定后来白赚了一个功能——加调试模式时，
  因为墨皇难度 / 战役关卡 / 队友全都从这一处推，**只改一行**就全解锁了。
  当初要是各存各的，就得挨个开后门。）
    同一份知识两份实现迟早对不上，这个项目已经因此出过三次 bug。
  - 固定主角把 Phase 1 校好的曲线整个打乱（第 2 关 97%、第 4 关 49%），
    逐关重搜 `enemyMod`。**当前曲线（5000 局/关，公平线 60.3%）**：
    93.7 → 86.0 → 78.8 → 74.2 → 65.8 → 59.0 → 50.1 → **41.6**，
    单调下降、最大偏差 2.2。
  - **这个游戏极度吃阵容克制，而且到处都是，不只是 BOSS 关。**
    普通的第 6 关，玩家 28 种阵容的胜率标准差就有 26.5、极差 87 个百分点；
    单人墨皇是 31.5 / 97，只是略高。
    **`npm run balance` 对随机阵容取平均，把这件事整个掩盖掉了**——
    它只能告诉你「角色平均有多强」，不能告诉你「这套阵容打那套阵容如何」。
    所以 `campaign-check.mjs` 现在逐个队友分别报胜率，只看均值会漏掉「带错人必输」。
  - 固定主角的一个副作用是**最差情况变好了**：随机 2 人打墨皇时最差组合只有 0.2%
    （守卫+牧师，纯纯打不动），固定剑士提供了伤害下限之后，最差的队友也有 12%。
  - `enemyMod` 调参时注意**存在真实断崖**：第 4 关 hp×0.84 是 75.3%、×0.85 是 66.7%，
    差 0.01 掉 8.6 个百分点。原因是「几刀砍得死」的整数阈值，不是噪声——
    多跑几次局数确认过。卡在缝里就取偏易的一侧。
- **战役敌人有身份 + 墨皇是真 BOSS + 剧情分段**（2026-08-25，`CAMPAIGN_PLAN.md` Phase 2/3/5）。
  - `createUnit(charId, player, slot, override)` 加了第四个参数，可覆盖
    name/color/hp/sp/atk/def/crit/dodge/spRegen/passive。**同一个角色、不同身份**
    靠它实现，不新增角色数据。另有 `unitSpec(entry)` 把「字符串 or {id,name,…} 对象」
    的解析收敛成一处，`battle.js` 和 `sim.js` 共用。
  - 16 个战役敌人都有了剧情名；`gameState` 拆成 `p2Picks`（纯 id，雷达图按 id 读）
    和 `p2Roster`（带身份的原始条目，建单位时用）。
  - **最终关改成墨皇独战**。依据是一条实测出来的机制事实：
    **本作回合流程是「双方各行动一个单位」，队伍人数不影响行动次数、只影响血池。**
    所以两人队里 BOSS 只能隔回合出手一次，单人则每回合都出手——
    「165 血的换皮术士」和「每回合都出手的 260 血墨皇」，只有后者像 BOSS。
    属性 260/22/8 是 `campaign-check.mjs` 校到 42% 的结果（实测 40.7%）。
  - **血量预算比想象中紧**：给墨皇 +40% 血就吃掉 24 个百分点。
    165/19/7 的双人版本只给玩家 6.1%，远超目标。
  - 过场支持多段文本（`intro`/`outro` 改成数组，字符串仍兼容），
    8 关剧情从不到 40 行扩到 52 段，并串了一条主线。
    **没做打字机效果**：分段推进本身就是节奏控制，再加打字机会让「点一下」
    产生两种含义（补完本段 vs 翻页），把最简单的交互复杂化。
  - 修掉两个既有 bug：(1) 战役赢了直接跳过场，**看不到任何战斗结算**——
    现在赢下一关也走完整结算界面，看完点「继续剧情 →」再进过场；
    (2) 「最终战役统计」显示的其实是**最后一关单场**数据——
    现在按关卡 id 存每关一份（`inkfight_campaign_totals`）、展示时求和。
    按 id 存而不是直接累加，是因为通关的关卡可以重打，重打只该覆盖那一关。
  - **修掉「`showResult` 会被调用两次」**：`checkVictory()` 有两个调用点
    （`nextTurn` 和「行动单位已阵亡」那条分支），胜负已定时两边**各排一个**
    `setTimeout(showResult, 700)`。正常速度下第二次在玩家还没点按钮时就跑完了，
    看不出来；但玩家只要在 700ms 内点掉「继续剧情」，延迟的第二次就会把他
    **从过场/通关界面拽回战斗结算界面**。加了 `gameState.resultShown` 幂等标记。
    这个 bug 是浏览器把标签页降频之后才现形的——**慢环境是照妖镜**。
  - 顺手修掉一个藏了很久的显示 bug：结算界面的数字滚动动画用
    `isNaN(parseInt(v))` 判断该不该滚，于是「22（守卫）」这种也被当数字滚了一遍，
    滚完括号里的名字就没了——「最高单次伤害」那行**一直没显示过是谁打的**。
    改成只滚纯数字（`/^[0-9]+$/`）。
- **战役难度曲线重排**（2026-08-24，`CAMPAIGN_PLAN.md` Phase 1）。
  起因：8 关的敌方阵容是按剧情随手写的，没人算过强度账，实测曲线是锯齿——
  最终 BOSS（62.5%）比第三关（40.1%）还好打，真正最难的是第七关（23.1%）。
  - **新尺子 `campaign-check.mjs`**：每关跑 N 局，报玩家胜率、目标偏差、单调性。
    和 `difficulty-check.mjs` 一样先量公平线（这次量到 60.8%）再报相对偏差。
  - **新旋钮 `enemyMod`**（`campaign.js` 数据 → `combat.js` 的 `applyStageMod`）。
    战役此前完全不吃 `applyDifficulty`，难度杠杆只剩 3 档 AI；
    现在多了一条 campaign 专用的属性路径。
    **两条路径不能合并**——叠加会让校准好的曲线整体跳变。
  - **当前曲线（5000 局/关，目标 ±2.5 以内，单调下降）**：
    92.4% → 83.8% → 75.9% → 73.4% → 64.0% → 57.1% → 47.9% → **39.5%**。
    阵容按实测强度排，每个角色恰好出场两次。
  - **推翻了一个想当然的代理指标**：「两名敌人在 `npm run balance` 里的胜率之和」
    预测不了关卡难度。实测 狂战士+守卫（和 93.5）只给玩家 41.9%，
    比 守卫+牧师（和 116.7，52.0%）还难打。**真正的驱动是队伍续航**——
    平均 15~23 回合的对局都是硬仗，6~8 回合的都是送。
    排关卡要用 `campaign-check.mjs` 实测，不要用胜率相加。
  - **场景 buff 本身就是很强的难度杠杆**，而且以前从没被当成杠杆用过。
    同样是 术士+牧师 + hard AI：在墨色虚空（无效果）玩家只有 26.2%，
    在赤焰熔岩（+15% 伤害）有 47.7%——**+15% 伤害正好破续航流**。
    校准时**必须锁定该关自己的场景**，用随机场景量出来的数会差 10 个百分点
    （这个坑当场踩到了）。
  - **选 atk 还是 hp 做旋钮，要看这一关的敌方靠什么赢。** 靠伤害的用 atk，
    靠续航的用 hp：第 8 关（术士+牧师）对 atk 几乎免疫
    （0.80~1.25 只从 39.1% 走到 24.8%），对 hp 却极敏感（0.80 → 58.4%）。
    项目原有的「攻击是最强杠杆」只在对称随机阵容下成立，不能照搬到具体关卡。
  - **简单 AI 拉不出低难度台阶**：不管配什么阵容都给玩家 90%~99%，
    所以 8 关里只有第 1 关用 easy，第 2 关起就是 normal / hard 加属性微调。

---

## 2026-08-26 —— 战斗深度重做（`COMBAT_PLAN.md`）

起因是用户玩完之后的一句话：

> 「其实策略性不够，所以打敌人的时候会不会输纯看比如什么负面有没有上之类的。
> 反正墨皇肯定是纯看运气了。」

诊断下来他是对的，而且比他说的还严重。

### 诊断：三个数字

- **玩家的全部技术只值 12.6 个百分点。** 把玩家替身从「完美」降到「一般」，
  胜率只差这么多（60.1% → 47.5%）。
- **一个玩家碰不到的骰子值 29.6 个百分点。** 眩晕全落空 58.3%、眩晕必中 87.9%。
  **骰子的话语权是玩家技术的 2.3 倍。**
- **58.4% 的回合在放同一个技能**，59.2% 的回合最优解甩开次优 30% 以上。
  最优打法一句话概括得完：有蓝放大招，没蓝平A。

为此新增五个诊断脚本：`depth-check` / `choice-check` / `skill-audit` /
`intent-value-check` / `campaign-tune`。

### 做了什么

1. **敌人意图公开 + 承诺制**（`intent.js`）。轮到玩家前，敌方下一击被算出、公开、
   **锁定**；轮到它时照做，哪怕已经变成臭棋。玩家于是能抢杀 / 布防 / 打断 / 换人接。
2. **把输出随机全部确定化**。眩晕 → 按 SP 阈值的确定性打断；暴击 → 蓄能条
   （期望伤害和概率模型完全一致，但零波动，而且玩家能看着条子攒大招）；
   被动闪避 → 恒定减伤。
3. **出手顺序交还玩家 + 轮空回蓝**。每回合自己挑派谁上，没出手的那个回复 SP。
4. **墨皇三阶段**（书写 / 涂改 / 重写），各考一种能力且各有解法。
5. **全面重校**：角色平衡差距 40.5 → 16.2；战役曲线单调下降、最大偏差 9.1；
   难度四档落在 87.4 / 68.8 / 51.3 / 35.7（目标 85 / 65 / 50 / 35）。

### ⚠ 实测否定的方案（**别再重走**）

**技能冷却 —— 单调有害。** 扫了四档强度：无冷却策略价值 46.1，×1.0 降到 36.6，
×1.5 降到 28.2。主流技能占比确实被压下去了（52.6% → 41.9%），
但那是**靠删选项换来的**——「乱按」的胜率从 19.4% 一路涨到 35.1%。
机制很清楚：**冷却把「什么时候用大招」这个判断从玩家手里拿走，交给了规则。**

**收紧 SP —— 方向反了。** ×0.4 给 37.3，×1.0 给 48.0，×3.0 给 64.0。
**SP 越充裕策略价值越高。** 和冷却同一个机制：约束削减选项，
而决策的价值正比于选项的数量与差异度。

**嘲讽 —— 三种设计全败。** 纯功能 +4.0 ／ 加护盾 +8.0 ／ 加伤害 +16.5 ／
加反弹 ×3.5 +9.7（数字是 `skill-audit` 里「禁掉它」的胜率变化，越正越糟）。
规律一致：**给它加赠品只会抬高评分让 AI 用得更勤，而嘲讽的效果本身是负的。**
根因是本作的回合结构——**行动次数不随存活人数变化**，所以别的战棋里
「保住脆皮让它继续输出」这个核心价值在这里根本不存在。
要救活嘲讽必须先动回合结构。结论写在 `combat.js` 的 `resolveTaunt` 注释里。

### 两个验收指标各自都能被钻空子

- **策略价值（完美−乱按）**会奖励「给乱按玩家更多搞砸的机会」。
  ×3.0 回蓝能拿到 64 分，但那时游戏是「永远按最大的那个键」——毫无深度。
- **主流技能占比**可以靠**删选项**刷好看。冷却就是这么把它从 52.6% 刷到 41.9% 的。

**两个必须一起看，而且都要配合「这样玩起来是什么感觉」的判断。**
计划前期把「主流占比 < 40%」当硬指标是个错误，追着它走反而伤害了真实深度。

### 又抓到四个「同一份知识两份实现」

这个项目的头号病因，这次一口气抓到四个，**全都是「文档写对了、其中一份实现错了」**：

1. **`sim.js` 的行动经济**。它用固定顺序数组跳过死人，等于「一方死一个，行动次数减半」；
   而 `battle.js` 是双方严格轮流。`CLAUDE.md` 明文写着规则，`campaign.js` 第 8 关注释
   也写着「单人则每回合都出手」——**设计意图对，但用来校准的工具不是这么算的**。
   后果：墨皇那关校到的 42% 是虚的，真实是 **1.9%**。
2. **字段名漂移**。`ai-scoring.js` 读 `threat.dmg`，`intent.js` 存的是 `estDmg`，
   恒定读到 `undefined`，威胁值永远 0，防御技能一个都没救活。
   **单测全绿、游戏也跑得好好的**，是靠埋点统计「为 0 的比例 = 100%」才挖出来的。
3. **被眩晕时的回合开始流程**。`battle.js` 整个跳过（不中毒、buff 不递减），
   `sim.js` 先跑再判。等于「被控住」还附赠中毒免疫和 buff 保鲜。
4. **轮空单位的状态衰减**。出手顺序改成自己挑之后，`processStartOfTurn` 只对出手的
   那个跑——板凳上的人冷却不递减，而墨皇的封印靠冷却解开，于是**永远解不开**，
   最后无技可用、`decide()` 返回 null、那个单位整局不动，胜率恒为 0%。

**教训：文档写清楚防不住这类 bug，只有「共用同一个函数」能防住。**

### 其它值得记的

- **`case 'damage'` 一直不看 `dot` / `debuff` / `corrupt`**，只算 `power`。
  于是「禁掉刺客的暗影突袭，胜率反而 +11.3」——省下的 SP 放毒刃更划算，AI 算不出。
  修完剑士 36.8% → 43.3%、刺客 55.3% → 64.3%。
- **`healSp` 只扣一半机会成本**，还白送「每点 SP 值 0.35 伤害」。改成全额 + 只在
  真缺蓝时计价之后，剑士 43.3% → 51.0%，狂战士「不屈」从死内容变成支柱。
- **杀人的不是攻击力，是「腐化爆发」**。调墨皇的 atk 从 8 到 22 胜率全是 0%，
  因为它的伤害根本不来自攻击力：22 伤害/层 × 5 层 × 全体 = **一发 220**，
  无视防御、不吃攻击力。这同时也是术士长期霸榜的原因。降到 12/层，两个问题一起解决。
- **测试里不要写死平衡数值**。`s.cost === 35`、`2*8` 这类断言会在每次调数值时假报错。
  改成读常量 / 按「最贵的那个」找。

### 最深层的性质变化：战斗现在几乎是确定性的

随机全部去掉之后，「固定阵容 vs 固定关卡 + 完美决策」的结果是**定的**。
`campaign-check` 的胜率于是不再是运气分布，而是
**「几个可选队友里有几个打得过」**——数字会量化到 1/3、2/3 这种台阶上。

后果：校准出现**极陡的断崖**（第 8 关 hp×0.75 → 33%、hp×0.70 → 83%），
±10 个百分点已经是这个结构下的合理精度。设计上这更接近 *Into the Breach* 那种
**解谜**而不是刷——一关解开一次就永远能过。真人不是完美玩家，实际体验仍有变数，
但这是本次重做最深层的变化，**值得单独想清楚要不要保留**。

---

## 2026-09-02 —— 战绩记录与分享（`archive/RECORDS_PLAN.md`）

打完一局，数字看一眼就没了。这次把每一局留下来，并且让它能带出这台电脑。

### 三层结构

```
core/sha256.js       SHA-256 / HMAC / base64url（浏览器和 Node 共用一份）
core/record.js       数据模型 + mvpOf + summarize + auditRecord
core/share-code.js   分享码编解码 + 签名
      ↓
game/save.js         三个 localStorage key（战绩 / 好友 / 本机档案）
view/records.js      战绩室界面
game/battle.js       showResult 里记一次账
```

分界线还是那条老规矩：能在 Node 里 import 的放 `core`，于是签名和校验全都被
`npm test` 覆盖到（新增 39 条，全仓库 311 → 378）。

### 为什么自己写 SHA-256

浏览器的 `crypto.subtle` 是异步的、且只在安全上下文里存在；Node 的 `node:crypto`
浏览器里没有。两边各挑一个现成的，就又是「同一份知识两份实现」——本项目的头号病因。
自己写一份，用 FIPS 180-4 和 RFC 4231 的**标准向量**钉住它。

这组标准向量挡的是一种很难自查的失败：**哈希算错了但一直自洽**。签名和验签用的是
同一份错实现，游戏里一切正常，直到某天换用标准库，才发现所有历史分享码全部作废。

### 防伪：两层，各挡一类人

纯前端游戏没有服务器，**密钥就在每个玩家的浏览器里**，做不到真的防伪。
能做的是把两种伪造的成本分别抬起来：

1. **HMAC-SHA256 签名** —— 挡「拿记事本把 230 改成 9999」。改一个字符就失效。
   实测抽查了分享码里 40 个位置逐个改，全部被拒。
2. **自洽审计 `auditRecord`** —— 挡「读过源码、能重新签名」的人。
   一局战斗的数字之间有真实约束：分项之和要等于总计、最高单击不能超过本人
   一整局的总伤害、角色要在花名册里、模式决定了「我」只能坐哪一边。
   **MVP 不进分享码**，永远由 `mvpOf` 从各单位数据现算——这一项因此伪造不了。
   实测：改了数字再重签，界面显示「✔ 签名有效 / ✖ 数据不自洽」，
   并指出「1 方的伤害分项之和 10252 对不上总计 483」，不入库。

**界面上如实写了这条边界**（`SHARE_TRUST_NOTE`）。写一个假的「已认证 ✓」
比不做校验更糟：玩家会真的相信它。

### 一条被否掉的审计规则

「击杀数之和 = 阵亡单位数」看着天经地义，实际会**把真实对局判成伪造**：
腐化爆发、瘟疫、狂暴自伤、墨蚀造成的死亡都不记击杀。用户 2026-09-02 那局
（补录进去的那一条）正是 4 人全灭、只有 2 次记名击杀。

由此定下 `auditRecord` 的写法：**宁可漏判，不可误判**。一条会对真实对局报警的规则，
会让玩家学会无视警告，整套校验就废了。同理编制不能写死「每方 4 人」——
战役是 2v2，最终关还是墨皇 1 打 2。

### 顺手收敛掉的两处重复

- **MVP 的算法**原本写在 `battle.js` 的 `renderStatsPanel` 里，战绩室要显示同一个人。
  抄第二遍的后果是「结算说 MVP 是弓手、战绩里说是刺客」，而两边都不报错。
  搬进 `core/record.js` 的 `mvpOf`，顺手修掉一个边界：全员 0 分时旧写法从
  `{score:0}` 起 reduce，会返回一个没有 name 的空壳，面板上显示成「undefined ⭐」。
- **难度名 `DIFF_LABEL`** 原本在 `battle.js`，搬进 `core/combat.js`，
  和 `DIFFICULTY_MODS` 放一起——难度的一切都在那一个文件。

### 分享码为什么这么短

单局 4v4 **336 个字符**，能直接贴进聊天框。做法是把角色 / 场景 / 模式 / 难度
都编成花名册里的下标，单位名与角色默认名相同时干脆不存（只有战役的剧情名是例外），
记录编成定长数组省掉所有键名。约 900 字节的 JSON 压到约 250。

**代价写在 `CLAUDE.md` 里了：那几张顺序表只能往末尾追加。** 往中间插一项，
所有已经发出去的分享码都会解成另一个角色，而且不报错。

### 实测时绕过的一个环境坑

内置浏览器把隐藏标签页的 `setTimeout` 钳到 1 秒，一整局要跑好几分钟。
临时把 `setTimeout` 换成 MessageChannel 驱动的虚拟时钟（**保留相对先后顺序**，
只是不等真实时间），一整局 4 秒跑完，人机、观战两条路径都完整跑通了。
以后在这个环境里做长流程 UI 实测，用这一招，别硬等。

## 2026-09-05 · 一念成锋：立绘进入战场

- 首页重做为水墨场景、人物立绘与可切换的 16 人名册；选择角色后可直接开始
  普通人机，队伍人数仍读取 `teamSizeFor`，随机队友与敌方仍用共用的 Fisher-Yates 洗牌。
- 选人和实战接入已有 WebP 立绘；战场采用分层地面、人物呼吸、突进、受击与阵亡姿态，
  数值面板独立于人物运动。图片丢失时战斗角色回退 Canvas 小人。
- 敌方预告连线只读取 `enemyIntent`，没有新增战斗规则；可选人物支持 Tab 聚焦及 Enter/空格确认。
- 新增不阻塞战斗的技能名演出；重做弧形刀光和按时间运动的受击碎屑，修复重击音效与
  `spawnCritBurst` 被吞进注释的老问题。浏览器验收全程静音，音效是否好听未作听感验收。
- 修复首页首次点击直接开战时菜单音乐覆盖战斗音乐的入口顺序；扰乱说明读取规则常量，
  双人模式人数说明读取 `teamSizeFor`。
- 平衡基线：`npm run balance 4000`，参战次数极差 8.1%，平均 20.1 回合，超时 0%；
  守卫 57.8%、牧师 45.7%。这是随机阵容困难 AI 基线，本轮未调数值。
- 验证：`npm test` 380/380，`git diff --check` 通过；浏览器验证首页换人后开局、立绘四人选择、
  ESC 取消目标、Enter 出手、三个场景、390px 窄屏、观战暂停/单步/4倍速、18 回合完整结算及同阵容重开。
  浏览器未捕获错误。手机布局适配有检查，音效听感和图片故障回退未作浏览器故障注入验收。
- 截图：`docs/screenshots/2026-09-05-title.png`、`docs/screenshots/2026-09-05-battle.png`。


## 2026-09-05 · 墨塑新生：三维角色、造物间与原创配乐

- 16 人原创低多边形几何模型，复用角色的体型、配色、头饰与武器配置；关节节点驱动待机、出手与倒地。
  三个场景使用浮空擂台、碎岩、刻线、光源和实时阴影。角色名字可点击并支持键盘操作。
- 实战默认进入 3D，支持拖动旋转、滚轮缩放和视角归位；窄屏按画面宽度自动退远镜头。
  3D / 立绘切换保留当前对局，偏好写入 inkfight_presentation；WebGL 初始化失败或上下文丢失回退立绘。
  模型读取现有 gameState，点击转给既有选人/目标入口，伤害特效使用三维投影位置；未改规则或平衡参数。
- 新增 model-workshop.html：16 人近看、布景切换、动作预览、自动环绕和 GLB 导出。
  文件保留静止网格、PBR 材质与独立关节，不含蒙皮骨骼或动画片段。
- 原创主题《墨起》《听泉破阵》《悬墨》《赤烬行》使用拨弦、笛音、铺底、低音和打击乐。
  配器随回合、死亡与总体血量分三层；正式播放与离线试听共用 music-score.js 和 music-engine.js。
  改用前瞻调度，静音/后台停止调度，恢复时重启对应主题；保留 BGM / SFX 独立音量和静音持久化。
- Three.js 0.180.0 的官方 npm 包下载到 E:\CodexAssets\inkfight；本地引擎文件和 MIT 许可随仓库提交，无 CDN 请求。
  原有启动方式不变。Khronos 验证器也放在 E 盘，仅用于验证，不是运行时依赖。
- 验证：npm test 402/402；16 人待机/出手/倒地几何边界、GLB 缓冲/索引、配器层与 WAV 格式检查通过。
  Khronos glTF Validator 对全部 16 人给出 0 错误、0 警告（信息级提示为默认矩阵可省略）。
- 浏览器：遍历全部 16 个模型、三个造物间布景和动作；完成模型点选→技能→敌方模型目标→伤害→AI 接招。
  测试 3D / 立绘同局切换、旋转、暂停、单步、4×、15 回合完整观战结算及同阵容重开，无捕获的 JS 错误。
  390px iframe 布局检查发现并修复边缘角色裁切；这不是手机硬件性能测试。
- 音频：四首 32 秒离线试听峰值 0.268–0.491，未削波，尾部淡出；实时测试增益为零，检查菜单/后台/恢复/场景/静音/取消静音/停止。
  未进行人耳听感验收。GLB 未逐一导入第三方建模软件，WebGL 故障回退未做浏览器故障注入。
- 资产：docs/screenshots/2026-09-05-{model-workshop,battle-3d,mobile-3d}.png；
  docs/models/ 的刀娘、墨鸦 GLB 与完整验证报告；docs/media/ 的灵泉 WAV 与音频检查报告。
