# docs · 导航

**只放两类东西：还在推进的计划，和以后还会查的参考。** 做完就不再看的计划
移进 `archive/`（那边的 `README.md` 列了每份还值得翻回来的理由）。

## 还在推进

| 文件 | 状态 |
|---|---|
| `ART_PLAN.md` | **进行中**。三张场景与可维护接入已完成；角色和音乐仍待后续决策 |
| `SCENE_WORKFLOW.md` | 新增/替换场景时使用：生成模板、接入配置、压缩、测试与浏览器验收 |
| `CHARACTER_ART_PLAN.md` | **仅方案**。战斗小人的推荐路线、16 人视觉身份证、AI 试片边界与停损点 |
| `CHARACTER_WORKFLOW.md` | 新增/修改角色小人时使用：视觉身份证、接入步骤、预览与测试标准 |
| `COMBAT_PLAN.md` | **任务 0~5 已完成，任务 6 站位系统暂缓**（有明确触发条件，见文末）。另记着**三条实测否定**的方案（技能冷却 / 收紧 SP / 嘲讽的四种设计）——动手前先看，别重走 |

## 长期参考

| 文件 | 用途 |
|---|---|
| `ROSTER_PLAN.md` | **加新角色时读这份**。有现有 16 人各自占着哪个机制空位的表，原则是「每个新角色占一个没人占的位置，不做换皮」 |
| `HISTORY.md` | 历次改动的完整经过、实测数字和当时的推理。想知道某个数值为什么是现在这个值，来这里翻 |

约束和铁律不在这里，在根目录 `CLAUDE.md`——那份每次会话都会整份读进上下文。
